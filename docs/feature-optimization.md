# Feature Optimization

> This file tracks ways to REFINE features that already exist in this project.
> It is NOT a roadmap of new features to add — nothing here introduces new
> functionality. Every item makes an existing feature cleaner, faster, safer,
> more accessible, or otherwise better.
>
> Maintained by the `/optimize-features` command. Last full inventory: 2026-09-05

## Feature Inventory

> **Numbering is stable, not sequential-by-group.** Features 1-41 keep the numbers
> they were assigned in the 2026-06 inventory so the findings recorded under each
> `### N.` heading below stay attached to the right feature. Features discovered
> since are appended at 42+ inside whichever category they belong to.
>
> **`Last reviewed`** means "last given a refinement analysis", not "last seen".
> `not yet` marks a feature that has been inventoried but never analyzed.

### A. Content & marketing pages

| #   | Feature                        | What it does                                                                                                                  | Where it lives                                                                                             | Last reviewed |
| --- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------- |
| 1   | Home                           | Hero + sticky scroll-storytelling (8 key points via GSAP ScrollTrigger) -> CTA                                                | `src/pages/Home.tsx`, `src/components/home/`                                                               | 2026-06-15    |
| 2   | About / Personal Biography     | Christian's bio, career, leadership philosophy, credentials grid                                                              | `src/pages/About.tsx`, `src/components/Credentials.tsx`, `src/data/credentials.ts`                         | 2026-06-15    |
| 3   | Altivum Inc                    | Company page (Altivum Logic, Vanguard)                                                                                        | `src/pages/Altivum.tsx`                                                                                    | 2026-06-15    |
| 4   | The Altivum Foundation         | Foundation mission, programs, support                                                                                         | `src/pages/Foundation.tsx`                                                                                 | 2026-06-15    |
| 5   | The Vector Podcast             | Episodes, guests, subscribe platforms                                                                                         | `src/pages/Podcast.tsx`, `src/components/podcast/`, `EpisodeCard.tsx`, `SpotifyFacade.tsx`                 | 2026-06-15    |
| 6   | Beyond the Assessment          | Book landing page                                                                                                             | `src/pages/BeyondTheAssessment.tsx`                                                                        | 2026-06-15    |
| 7   | AWS                            | AWS Community Builder page                                                                                                    | `src/pages/AWS.tsx`                                                                                        | 2026-06-15    |
| 8   | Claude                         | AI work / Anthropic page                                                                                                      | `src/pages/Claude.tsx`                                                                                     | 2026-06-15    |
| 9   | Links                          | Link-in-bio hub                                                                                                               | `src/pages/Links.tsx`                                                                                      | 2026-06-15    |
| 10  | Contact & Speaking             | Contact form + speaking/press info                                                                                            | `src/pages/Contact.tsx` (+ contact endpoint)                                                               | 2026-06-15    |
| 11  | Privacy Policy                 | Privacy/legal page + consent reset control                                                                                    | `src/pages/Privacy.tsx`                                                                                    | 2026-06-15    |
| 12  | 404 / Not Found                | Catch-all error page                                                                                                          | `src/pages/NotFound.tsx`                                                                                   | 2026-06-15    |
| 42  | Hero intro video               | CDN-served animated brand intro with same-origin poster + preload, CSP-aware                                                  | `src/components/home/HeroIntroVideo.tsx`, `customHttp.yml` (`media-src`)                                   | not yet       |
| 43  | AEO answer surfaces            | Direct-answer summaries, question-shaped headings and visible FAQ blocks whose text matches the FAQPage JSON-LD byte-for-byte | `src/components/aeo/`, `src/data/aeoSummaries.ts`, `scripts/validate-prerender-seo.mjs` (VAL-AEO-001..008) | not yet       |
| 44  | Internal linking & breadcrumbs | Cross-link bands between related pages + breadcrumb trails wired to BreadcrumbList schema                                     | `src/components/CrossLinkBand.tsx`, `src/components/Breadcrumbs.tsx`                                       | not yet       |

### B. Blog system

| #   | Feature                          | What it does                                                                                                                                | Where it lives                                                                                                          | Last reviewed |
| --- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------- |
| 13  | Blog listing                     | Series/category/featured filtering from Sanity                                                                                              | `src/pages/Blog.tsx`, `src/sanity/queries.ts`                                                                           | 2026-09-05    |
| 14  | Blog post reading                | Portable Text, Shiki highlighting, reading progress, image CLS fix                                                                          | `src/pages/BlogPost.tsx`, `src/sanity/PortableTextComponents.tsx`, `HighlightedCodeBlock.tsx`, `ReadingProgressBar.tsx` | 2026-09-05    |
| 15  | RSS feed                         | Sanity posts -> `dist/rss.xml`                                                                                                              | `scripts/generate-rss.js`                                                                                               | 2026-09-05    |
| 45  | Sanity content data layer        | Typed client + GROQ queries, TTL caches for listing and per-post, runtime shape guards, typed errors, schema-drift and N+1 regression tests | `src/sanity/` (`client.ts`, `queries.ts`, `cache.ts`, `postCache.ts`, `guards.ts`, `errors.ts`, `schemas.ts`)           | 2026-09-05    |
| 46  | Blog preview & editorial tooling | Build-time preview extraction + heading rewrite pass over raw editorial source                                                              | `scripts/generate-blog-previews.js`, `scripts/rewrite-blog-headings.mjs`, `scripts/editorial-raw/`                      | 2026-09-05    |

### C. Alti - AI agent

| #   | Feature                      | What it does                                                                                                         | Where it lives                                                                                                                            | Last reviewed |
| --- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| 16  | Alti chat (frontend)         | Full-page `/chat` + floating widget, shared engine, sessionStorage sync, capability intro, context-aware suggestions | `src/pages/Chat.tsx`, `src/components/chat/`, `src/hooks/useChatEngine.ts`                                                                | 2026-09-05    |
| 17  | Alti backend                 | Streaming agent (Strands + Bedrock Haiku), 9 tools, KB RAG, event protocol, guardrail                                | `lambda/chat-stream/`                                                                                                                     | 2026-09-05    |
| 18  | Alti 3D mascot               | R3F mascot in widget button + canvas safety / WebGL fallback                                                         | `src/components/chat/AltiMascot.tsx`, `SafeCanvas.tsx`, `public/alti.glb`                                                                 | 2026-09-05    |
| 19  | Visitor memory               | Per-device fact memory, PII-sanitized, 90-day TTL, welcome-back + in-widget inspection and forget-me                 | `lambda/chat-stream/memory.mjs`, `memory.schema.mjs`, `src/utils/deviceId.ts`                                                             | 2026-09-05    |
| 20  | Knowledge Base + sync        | Bedrock KB retrieval + S3-event ingestion                                                                            | `lambda/chat-stream/kbRetrieve.mjs`, `lambda/kb-sync/`                                                                                    | 2026-09-05    |
| 47  | Generative UI blocks         | `render_ui` tool + a Zod-defined block vocabulary rendered as timeline/comparison/etc. surfaces on `/chat`           | `lambda/chat-stream/uiBlocks.mjs`, `genUi.mjs`, `tools/renderUi.mjs`, `src/components/chat/GenerativeBlocks.tsx`, `src/utils/uiBlocks.ts` | 2026-09-05    |
| 48  | Podcast RAG / Ask the Vector | Second KB over episode transcripts + `search_podcast` tool + an on-page ask surface with episode citations           | `lambda/chat-stream/podcastRetrieve.mjs`, `tools/searchPodcast.mjs`, `src/components/podcast/AskTheVector.tsx`                            | 2026-09-05    |

### D. Other interactive / platform features

| #   | Feature                     | What it does                                                                                             | Where it lives                                                                                           | Last reviewed |
| --- | --------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ------------- |
| 21  | Blueprint generator         | Architecture generator (Opus + Haiku verdict), golden-example RAG, Mermaid render                        | `src/pages/Blueprint.tsx`, `src/components/blueprint/`, `lambda/blueprint/`, `src/hooks/useBlueprint.ts` | not yet       |
| 22  | KB Admin console            | Cognito-gated CRUD on Sanity `kbEntry` -> `knowledge-base.txt` -> S3                                     | `src/pages/Admin.tsx`, `src/components/admin/`, `lambda/kb-builder/`                                     | not yet       |
| 23  | Site Health dashboard       | Live metrics/alarms view on `/admin`                                                                     | `src/hooks/useSiteHealth.ts`, `src/components/admin/SiteHealthPanel.tsx`                                 | not yet       |
| 24  | Newsletter signup           | Signup form + CTA (reused for Blueprint waitlist)                                                        | `src/components/NewsletterForm.tsx`, `NewsletterCTA.tsx` (+ newsletter endpoint)                         | not yet       |
| 25  | Public MCP server           | `ask_alti` tool over KB + Bedrock + guardrail; install badge                                             | `lambda/mcp-server/`, `src/components/McpInstallBadge.tsx`                                               | not yet       |
| 26  | Social proof / testimonials | Testimonial display                                                                                      | `src/components/Testimonials.tsx`, `src/data/testimonials.ts`                                            | not yet       |
| 49  | AWS infrastructure topology | Interactive 3D topology of the live AWS estate with 2D and non-WebGL fallbacks                           | `src/components/aws/`, `src/data/infrastructureTopology.ts`                                              | not yet       |
| 50  | Claude Architecture X-Ray   | Interactive request-pipeline diagram with a live trace input that walks a real request through the nodes | `src/components/claude/`, `src/data/architectureNodes.ts`                                                | not yet       |

### E. Cross-cutting site systems

| #   | Feature                       | What it does                                                                                                                          | Where it lives                                                                                                                                        | Last reviewed |
| --- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| 27  | Navigation                    | Header + About dropdown, mobile hamburger, routes SSOT + drift test                                                                   | `src/components/Navigation.tsx`, `src/routes.ts`, `src/routes.test.ts`                                                                                | not yet       |
| 28  | Design system & motion        | Typography/colors, Lenis smooth scroll, View Transitions, scroll reveals, micro-interactions                                          | `src/index.css`, `src/utils/typography.ts`, `LenisProvider.tsx`, `ViewTransitionLink.tsx`, `SplitReveal.tsx`, `FadeReveal.tsx`, `src/utils/motion.ts` | not yet       |
| 29  | SEO                           | Meta tags, canonical, OG/Twitter cards, JSON-LD `@graph` schemas, OG-card routing                                                     | `src/components/SEO.tsx`, `src/utils/schemas.ts`, `src/utils/ogCards.ts`                                                                              | 2026-06-20    |
| 30  | Analytics & consent           | Plausible + Cloudflare + consent-gated PostHog product analytics behind a consent banner                                              | `src/components/ConsentBanner.tsx`, `src/utils/consent.ts`, `src/utils/posthog.ts`, `src/utils/analytics.ts`, `public/plausible-init.js`              | not yet       |
| 31  | Web Vitals & monitoring       | Vitals + CSP-report ingestion -> CloudWatch, alarms                                                                                   | `src/utils/webVitals.ts`, `src/utils/endpoint.ts`, `lambda/metrics/`                                                                                  | not yet       |
| 32  | Error handling                | Global + page-level error boundaries, pathname-keyed reset                                                                            | `src/components/ErrorBoundary.tsx`, `ErrorFallbacks.tsx`                                                                                              | not yet       |
| 33  | Security layer                | HMAC request signing, Turnstile-gated session tokens, rate limiting, Bedrock guardrail, fail-closed auth                              | `src/utils/sessionToken.ts`, `src/utils/turnstile.ts`, `lambda/session-token/`, `lambda/shared/` (`hmac`, `rateLimit`, `auth`, `requestAuth`)         | not yet       |
| 51  | Icon system                   | Self-hosted Material icon path data (no CDN) + typed `<Icon>` + a scan/extract/build pipeline that ships only the icons actually used | `src/components/icons/`, `scripts/scan-material-icons.mjs`, `extract-material-icons-svg.py`, `build-material-icon-paths.mjs`                          | not yet       |
| 52  | UI primitives                 | Shared Button / FormInput / IconButton with house styling, focus and tap-target rules                                                 | `src/components/ui/`                                                                                                                                  | not yet       |
| 53  | Feature flag system           | Typed flag registry replacing scattered `import.meta.env` checks, plus a dead-flag detector                                           | `src/utils/featureFlags.ts`, `src/hooks/useFeatureFlag.ts`, `scripts/check-dead-feature-flags.mjs`                                                    | not yet       |
| 54  | Frontend resilience & tracing | Timeout / retry-with-backoff / circuit breaker for browser API calls + per-request `X-Request-Id` correlated to Lambda logs           | `src/utils/resilience.ts`, `src/utils/traceId.ts`, `lambda/shared/timeout.mjs`                                                                        | not yet       |
| 55  | Error tracking & RUM          | Sentry error capture + CloudWatch RUM performance/breadcrumbs, with build-time source-map upload                                      | `src/utils/sentry.ts`, `src/utils/rum.ts`, `lambda/shared/errorTracking.mjs`, `scripts/upload-sourcemaps.mjs`                                         | not yet       |
| 56  | Structured logging            | Scoped, PII-redacting loggers mirrored across frontend and the Lambda fleet                                                           | `src/utils/logger.ts`, `lambda/shared/logger.mjs`                                                                                                     | not yet       |

### F. Build-time & ops

| #   | Feature                              | What it does                                                                                                                           | Where it lives                                                                                                                                                                            | Last reviewed |
| --- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| 34  | Build pipeline & env validation      | Fail-fast env + endpoint checks and Amplify build config                                                                               | `scripts/validate-env.js`, `scripts/validate-endpoints.mjs`, `amplify.yml`                                                                                                                | not yet       |
| 35  | Sitemap generation                   | Sanity posts -> `dist/sitemap.xml` (SSOT for the indexable set)                                                                        | `scripts/generate-sitemap.js`                                                                                                                                                             | not yet       |
| 36  | SSG prerendering                     | Build-time prerender crawl of static routes                                                                                            | `scripts/prerender.js`, `src/utils/prerender.ts`                                                                                                                                          | not yet       |
| 37  | Podcast episode generation           | YouTube API -> `generatedEpisodes.json` (static fallback)                                                                              | `scripts/generate-podcast-episodes.js`                                                                                                                                                    | not yet       |
| 38  | OG image generation                  | Build-time Open Graph card assets per route                                                                                            | `scripts/generate-og-images.mjs`, `scripts/og-assets/`                                                                                                                                    | not yet       |
| 39  | Podcast transcription & ingestion    | Transcribe episodes + ingest transcripts into the podcast KB                                                                           | `scripts/transcribe-podcast.mjs`, `transcribe-youtube.mjs`, `ingest-podcast-transcripts.mjs`                                                                                              | not yet       |
| 40  | Lambda deploy & verification tooling | Verified deploy + import smoke check + IAM drift + live smoke tests                                                                    | `scripts/deploy-lambda.sh`, `smoke-test-lambdas.mjs`, `iam-drift.sh`                                                                                                                      | not yet       |
| 41  | CI/CD & dependency security          | PR/push test+build gate, npm audit, weekly scheduled audit, Dependabot, greptile review, dependency-age check                          | `.github/workflows/ci.yml`, `security-audit.yml`, `dependency-age-check.yml`, `.github/dependabot.yml`                                                                                    | 2026-06-21    |
| 57  | E2E test suite                       | Cypress specs covering home, navigation, blog, chat, contact, 404 and mobile nav                                                       | `cypress/e2e/` (11 specs)                                                                                                                                                                 | not yet       |
| 58  | Build-time validation gates          | ~39 numbered VAL-* assertions over prerendered HTML: SEO, structured data, AEO, perf, security headers, caching, canvas fonts          | `scripts/validate-prerender-seo.mjs`, `validate-security-headers.test.mjs`, `validate-caching-headers.test.mjs`, `validate-security-hardening.test.mjs`, `validate-canvas-fonts.test.mjs` | not yet       |
| 59  | Deployment observability             | Deployment markers, post-deploy verification, source-map upload, error-insights workflow                                               | `scripts/mark-deployment.mjs`, `post-deploy-check.sh`, `upload-sourcemaps.mjs`, `.github/workflows/error-insights.yml`                                                                    | not yet       |
| 60  | External uptime canary               | CloudWatch Synthetics canary probing routes, feeds, chat backend, security headers, real-404 and serving-origin every 15 min -> SNS    | `infra/canary.yaml`                                                                                                                                                                       | not yet       |
| 61  | Code quality & tech-debt tooling     | knip (dead code), jscpd (duplication), syncpack (dep alignment), file-size and tech-debt budgets, flaky-test detection, quality report | `scripts/code-quality-report.mjs`, `check-tech-debt.mjs`, `check-file-sizes.mjs`, `detect-flaky-tests.mjs`, `check-lambda-coverage.mjs`                                                   | not yet       |
| 62  | Runtime security scanning            | ZAP DAST scan of the site + DAST API scan of the six HTTP Lambda services + secret scanning                                            | `.github/workflows/dast-scan.yml`, `dast-api-scan.yml`, `secret-scanning.yml`                                                                                                             | not yet       |
| 63  | Release & changelog automation       | Tagged releases + generated changelog + PR labeler                                                                                     | `.github/workflows/release.yml`, `labeler.yml`, `scripts/generate-changelog.mjs`                                                                                                          | not yet       |
| 64  | API documentation generation         | TypeDoc for source + OpenAPI docs assembled from the seven per-Lambda specs                                                            | `scripts/generate-openapi-docs.mjs`, `lambda/*/openapi.yaml`, `docs/api/`                                                                                                                 | not yet       |
| 65  | Build performance & size tracking    | Timed builds with run-over-run comparison, bundle-size and Lambda-size budgets                                                         | `scripts/track-build-performance.mjs`, `compare-build-performance.mjs`, `track-bundle-size.mjs`, `track-lambda-size.mjs`                                                                  | not yet       |
| 66  | Alarms & dashboard provisioning      | Scripted CloudWatch alarm fleet + shared dashboard definition                                                                          | `scripts/setup-alarms.sh`, `setup-dashboard.sh`, `cloudwatch-dashboard.json`                                                                                                              | not yet       |

## Optimization Opportunities

**Groups B + C implemented 2026-09-05** — 181 of the 182 findings landed across two waves of file-ownership-partitioned agents (15 clusters, then 6 for the cross-boundary follow-ups), plus a hand-finished security fix and privacy flag. 1 finding is deferred (marked inline). Verified end to end, not self-reported: ESLint `--max-warnings 0` clean, `tsc --noEmit` and `tsc:lambda` clean, 1959 frontend tests and 664 Lambda tests passing (from 1681 / 586 at baseline), `npm run build` exit 0 with 31 routes prerendered and 0 seo-gate violations, and all six Lambda bundles passing the deploy script's cold-start import check. The RSS parity gate and the new `omitDeviceId` flag were each proven to FAIL without their fix before being accepted.

> Populated per feature as `/optimize-features` reviews them. Each item:
> `- [ ] **[Lens]** <title> — <summary>. Impact: <H/M/L>. Effort: <L/M/H>. (file:line) — added <date>`
> Completed items are checked off with a completion date rather than deleted.

**Group A (content & marketing pages) reviewed 2026-06-15** — 61 verified refinements via parallel review + adversarial verification.

**Feature 29 (SEO) reviewed 2026-06-20** — 37 verified refinements (33 after dedup: 7 Medium, 26 Low) via an 8-dimension parallel audit (structured-data, head/meta, crawl-infra, on-page content, perf-head, code/arch, tests, AI/GEO) with per-finding adversarial verification against current 2025-2026 Google/schema.org standards. 0 findings rejected; several High claims honestly downgraded after the live site refuted the premise (e.g. Amplify serves noindex routes with a real 404 status).

**Groups B (blog system) + C (Alti AI agent) reviewed 2026-09-05** — 182 verified refinements across 12 features (3 High, 68 Medium, 111 Low), from 186 raw findings over 50 lens passes. Every finding faced an adversarial verifier that re-opened the cited lines and checked four axes (evidence real, refinement-not-new-feature, impact honest, fix sound); 12 were rejected on the merits. The 3 High claims each faced two further independent skeptics on reachability and blast radius. Two completeness critics then named 8 unexamined areas, whose own finders added 8 further verified findings (marked _gap sweep_).

### 1. Home

- [x] **[Accessibility]** Heading outline skips h1→h3 with the lone h2 out of order — sr-only h1, then 8× `as="h3"` keyPoints, then the CTA h2; primary content has no parent h2. Demote keyPoints to h2 or add a visually-hidden section h2 (`SplitReveal` `as` union needs `'h2'`). Impact: Medium. Effort: Low. (`src/pages/Home.tsx:57-66`, `src/components/SplitReveal.tsx:14`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Performance]** Below-the-fold profile image eager-loads and competes with the LCP hero — add `loading="lazy" decoding="async"`. Impact: Medium. Effort: Low. (`src/pages/Home.tsx:158-163`) — added 2026-06-15 — **completed 2026-06-15 (WP-D)**
- [x] **[Cleaner code]** Dead conditional branch — the `point.link` ternary's `<div>` fallback is unreachable (every keyPoint has a link). Impact: Low. Effort: Low. (`src/pages/Home.tsx:89-93`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Architecture]** Static `keyPoints` array and `renderTab` are reconstructed on every render — hoist `keyPoints` to module scope. Impact: Low. Effort: Low. (`src/pages/Home.tsx:30-41`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** keyPoint tabs keyed by array index while sibling lists key by content. Impact: Low. Effort: Low. (`src/pages/Home.tsx:84`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** Hero shader comment documents the wrong navy hex (`// #1A2332` vs actual `#2E4A6B`). Impact: Low. Effort: Low. (`src/components/home/heroShader.ts:32`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 2. About / Personal Biography

- [x] **[Consistency]** "Altivum Inc." and "Beyond the Assessment" are plain text — wrap in `ViewTransitionLink` to `/altivum` and `/beyond-the-assessment`. Impact: Medium. Effort: Low. (`src/pages/About.tsx:52-53,74,83-84`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Performance]** Hero signature image has no width/height/aspect-ratio (CLS risk) — add `width={1500} height={1500}`. Impact: Low. Effort: Low. (`src/pages/About.tsx:34-39`) — added 2026-06-15 — **completed 2026-06-15 (WP-D)**
- [ ] **[Performance]** 1500×1500 hero PNG ships full-res for a ≤768px box (mostly-transparent canvas) — pre-resize/crop the asset. Impact: Low. Effort: Low. (`src/pages/About.tsx:34-39`) — added 2026-06-15
- [x] **[Cleaner code]** Italic via inline `style={{ fontStyle: 'italic' }}` instead of the Tailwind `italic` class used everywhere else. Impact: Low. Effort: Low. (`src/pages/About.tsx:74`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Accessibility]** Hero image alt ("My Personal Biography") duplicates the sr-only h1 and misses the mark's text — tighten or set `alt=""`. Impact: Low. Effort: Low. (`src/pages/About.tsx:36,48`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 3. Altivum Inc

- [x] **[Consistency]** CTAs/external links drop the standard button recipe (`min-h-[48px] touch-manipulation active:scale-[0.98]`, gold glow) that Foundation uses. Impact: Medium. Effort: Low. (`src/pages/Altivum.tsx:408-421`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** Branch cards & HQ node bypass `typography.ts` with raw `text-*`/`leading-*` utilities. Impact: Medium. Effort: Low. (`src/pages/Altivum.tsx:255-309`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** Each `timelineItems` entry defines an `icon` SVG that is never rendered — render it or drop the field. Impact: Medium. Effort: Low. (`src/pages/Altivum.tsx:13-17,344-362`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** The three "Imperatives" are authored verbatim twice (timeline + Mission) — hoist to one array (cf. Foundation `FOCUS_AREAS`). Impact: Medium. Effort: Low. (`src/pages/Altivum.tsx:52-71,383-396`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[UI]** Hero badges positioned with magic `bottom-50` (200px) — can crowd the logo on short viewports; use an intentional corner offset. Impact: Low. Effort: Low. (`src/pages/Altivum.tsx:215,228`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Performance]** LCP hero logo has no width/height (verifier scoped to hero only; AWS logo & full-screen image already reserve boxes). Impact: Low. Effort: Low. (`src/pages/Altivum.tsx:203-208`) — added 2026-06-15 — **completed 2026-06-15 (WP-D)**
- [x] **[Accessibility]** Chamber-recognition anchor needs an `aria-label` with new-tab/destination context (decorative-SVG portion mostly moot). Impact: Low. Effort: Low. (`src/pages/Altivum.tsx:224-232`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 4. The Altivum Foundation

- [x] **[Consistency]** Cybersecurity is a 4th funded field (grid + FAQ + schema) but dropped from H1, meta description, keywords, and Vision copy — align the crawlable/snippet copy to the four-field model. Impact: High. Effort: Low. (`src/pages/Foundation.tsx:64,35-36,103`; `src/utils/schemas.ts:531,561`) — added 2026-06-15 — **completed 2026-06-15 (WP-B; also aligned the JSON-LD `description` at schemas.ts:551)**
- [x] **[Performance]** Hero LCP JPEG ships 175 KB un-re-encoded with no WebP variant — convert to q80 WebP (AWS.tsx convention). Impact: Medium. Effort: Low. (`src/pages/Foundation.tsx:3,49-54`) — added 2026-06-15 — **completed 2026-06-15 (WP-D; 175 KB → 106 KB, orphaned JPEG removed)**
- [x] **[Accessibility]** The four focus areas are a `<div>` grid, not a semantic list — wrap in `<ul>`/`<li>`. Impact: Low. Effort: Low. (`src/pages/Foundation.tsx:149-166`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** Four near-identical CTA button blocks hand-duplicated — extract local button variants. Impact: Low. Effort: Low. (`src/pages/Foundation.tsx:71-86,220-235`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 5. The Vector Podcast

- [x] **[Cleaner code]** Episode dates render one day early for US visitors — `new Date('YYYY-MM-DD')` parses as UTC; make `formatDate` TZ-safe (cf. existing `formatMonthYear`). Impact: High. Effort: Low. (`src/utils/dateFormatter.ts:8`, `src/components/EpisodeCard.tsx:53,72,179`) — added 2026-06-15 — **completed 2026-06-15 (WP-A)**
- [x] **[UX]** Featured "Now Playing" details card omits Spotify/Apple/YouTube listen links that every other card shows — add the link row. Impact: Medium. Effort: Low. (`src/pages/Podcast.tsx:169-187`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Architecture]** EpisodeCard `topics`/`guests` sections are dead for production data (generator emits `topics: []`, never `guests`) — pick one source of truth. Impact: Medium. Effort: Medium. (`src/components/EpisodeCard.tsx:196,220`; `scripts/generate-podcast-episodes.js:261`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Resilience & tests]** EpisodeCard date test uses a midday-UTC fixture that masks the date bug and never asserts the rendered date — use bare `YYYY-MM-DD` + assert under a fixed TZ. Impact: Medium. Effort: Low. (`src/components/EpisodeCard.test.tsx:11,22`) — added 2026-06-15 — **completed 2026-06-15 (WP-A)**
- [x] **[Accessibility]** Compact episode accordion button has `aria-expanded` but no `aria-controls`/labelled region — add a panel id. Impact: Low. Effort: Low. (`src/components/EpisodeCard.tsx:24,66-67`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** `SpotifyFacade` iframe lacks the `sandbox` hardening `YouTubeFacade` applies (live-verify Spotify still plays). Impact: Low. Effort: Low. (`src/components/SpotifyFacade.tsx:13`, `src/components/YouTubeFacade.tsx:30`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 6. Beyond the Assessment

- [x] **[Performance]** Hero + reading images lack intrinsic width/height — CLS on a tracked metric; add `1500×1500` / `1131×1600` (Home pattern). Impact: High. Effort: Low. (`src/pages/BeyondTheAssessment.tsx:31-36,52-56`) — added 2026-06-15 — **completed 2026-06-15 (WP-D)**
- [x] **[Architecture]** 2.1 MB unused source EPUB committed in `src/assets/` (56 XHTML files, never imported) — remove from repo. Impact: Low. Effort: Low. (`src/assets/Beyond the Assessment FINAL.epub/`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** CTA hover uses stock `amber-400` instead of `hover:bg-altivum-gold/90`. Impact: Low. Effort: Low. (`src/pages/BeyondTheAssessment.tsx:94`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Accessibility]** CTA omits touch-target/press-feedback classes; arrow SVG lacks `aria-hidden`. Impact: Low. Effort: Low. (`src/pages/BeyondTheAssessment.tsx:89-100`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** Mixed straight/curly quotes & apostrophes in body copy — normalize to typographic glyphs. Impact: Low. Effort: Low. (`src/pages/BeyondTheAssessment.tsx:61,75,78,81`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 7. AWS

- [x] **[Accessibility]** Keyboard overlay buttons (static `projectTo2D`) float over the wrong spots because the canvas auto-rotates — remove the invisible overlay layer; the visible cluster nav bar is the real accessible control. Impact: Medium. Effort: Low. (`src/components/aws/InfraTopology.tsx:98-123`, `TopologyScene.tsx:162,250`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Accessibility]** Overlay buttons are focusable children of an `aria-hidden="true"` wrapper with per-child `aria-hidden="false"` (unreliable anti-pattern) — resolved by removing the overlay. Impact: Medium. Effort: Low. (`src/components/aws/InfraTopology.tsx:99-108`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** `ClusterDetail.tsx` is dead code (162 lines, exported, never imported; pulls in drei `Html`) — delete. Impact: Medium. Effort: Low. (`src/components/aws/ClusterDetail.tsx:1,98`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** `flyTo` and `handleClusterClick` (and `reset`/`handleDeselect`) duplicate the camera-fly logic — extract `flyToCluster(id)`. Impact: Medium. Effort: Low. (`src/components/aws/TopologyScene.tsx:137,172`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Performance]** Hero + Community Builder images lack dimensions (CLS); below-fold banner eager-loads — add width/height + `loading="lazy"`. Impact: Medium. Effort: Low. (`src/pages/AWS.tsx:35-40,50-54`) — added 2026-06-15 — **completed 2026-06-15 (WP-D)**
- [x] **[Consistency]** Reduced-motion read via raw `window.matchMedia` (non-reactive) instead of `useMediaQuery`/`isMotionDisabled()`. Impact: Low. Effort: Low. (`src/components/aws/TopologyScene.tsx:58`, `ClusterEdge.tsx:15`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 8. Claude

- [x] **[Accessibility]** Keyboard-focused pipeline nodes have no visible focus indicator — inline `outline: 'none'` overrides the global gold focus-visible ring on the diagram's main control. Impact: High. Effort: Low. (`src/components/claude/PipelineNode.tsx:48,54`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** Architecture X-Ray shows stale guardrail config (Version "2") vs live `GUARDRAIL_VERSION = "5"` on a page marketed as production-real. Impact: Medium. Effort: Low. (`src/data/architectureNodes.ts:66`, `lambda/chat-stream/index.mjs:55`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Accessibility]** Streaming response bubble re-announces on every chunk via `aria-live="polite"` — gate to the settled response. Impact: Medium. Effort: Low. (`src/components/claude/TraceResponseBubble.tsx:9-12`, `ArchitectureXRay.tsx:297,437`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** Motion gating uses raw `matchMedia` in two places instead of `isMotionDisabled()` (misses prerender flag). Impact: Low. Effort: Low. (`src/components/claude/ArchitectureXRay.tsx:122,196`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** Duplicate node-state builders (`buildInitialNodeStates`/`resetAllNodeStates` identical + a 3rd inline copy) and a redundant double `abort()`. Impact: Low. Effort: Low. (`src/components/claude/ArchitectureXRay.tsx:25-39,222,248`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 9. Links

- [x] **[Architecture]** `SOCIAL_LINKS` imported but bypassed for all but one entry — already caused live drift (`x.com/x_thechrisgrey` vs constant `x.com/thechrisgrey`). Reconcile the X URL, then source all from the constant. Impact: Medium. Effort: Low. (`src/pages/Links.tsx:43-44,5`; `src/constants/links.ts:9`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J + X-handle reconcile): all links sourced from SOCIAL_LINKS + social cards deduped; owner confirmed canonical X handle = x.com/thechrisgrey, reconciled across Links.tsx + schemas.ts (Person sameAs)**
- [x] **[Consistency]** Contact CTA uses raw `<a href="/contact">` instead of `ViewTransitionLink`. Impact: Medium. Effort: Low. (`src/pages/Links.tsx:278-283`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Cleaner code]** Personal & company social-card grids are byte-identical markup — extract a `SocialCard`/section helper. Impact: Low. Effort: Low. (`src/pages/Links.tsx:201-224,239-262`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Performance]** Featured QR image has no width/height/loading/decoding (below fold) — add them (confirm intrinsic size). Impact: Low. Effort: Low. (`src/pages/Links.tsx:133-137`) — added 2026-06-15 — **completed 2026-06-15 (WP-D)**

### 10. Contact & Speaking

- [x] **[Accessibility]** Field validation errors aren't associated with the input (one shared alert; no `aria-invalid`/`aria-describedby`/focus) — track per-field errors (reuse `FormInput` pattern). Impact: High. Effort: Medium. (`src/pages/Contact.tsx:55-66,264-326`; `src/components/ui/FormInput.tsx:41-49`) — added 2026-06-15 — **completed 2026-06-15 (WP-C)**
- [x] **[Accessibility]** Inputs use `focus:outline-hidden`, suppressing the global gold focus-visible ring — drop it (mouse case already handled in CSS). Impact: Low. Effort: Low. (`src/pages/Contact.tsx:273,290,306,324`) — added 2026-06-15 — **completed 2026-06-15 (WP-C)**
- [x] **[Accessibility]** Success-modal confirmation text isn't in a live region — add `role="status" aria-live="polite"`. Impact: Low. Effort: Low. (`src/pages/Contact.tsx:531-537`) — added 2026-06-15 — **completed 2026-06-15 (WP-C)**
- [x] **[Cleaner code]** Dead `'success'` status branch — `formStatus` is never set to `'success'` (success path goes `'idle'` + modal). Impact: Low. Effort: Low. (`src/pages/Contact.tsx:20-23,344-352`) — added 2026-06-15 — **completed 2026-06-15 (WP-C)**
- [x] **[Architecture]** Six near-identical contact-info link blocks (~75 lines) should be data-driven (siblings already `.map`). Impact: Low. Effort: Low. (`src/pages/Contact.tsx:391-468`) — added 2026-06-15 — **completed 2026-06-15 (WP-C)**

### 11. Privacy Policy

- [x] **[Consistency]** "contact form" link is a raw `<a href>` (only raw internal anchor in `src/`) — use `ViewTransitionLink`. Impact: Medium. Effort: Low. (`src/pages/Privacy.tsx:306`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Accessibility]** Manual `•` bullet spans are announced by AT — add `aria-hidden` or use native `list-disc marker`. Impact: Low. Effort: Low. (`src/pages/Privacy.tsx:71-72,104-105,198-199,226-227`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[UX]** "Reset analytics preference" button always renders, even with no consent on file — gate behind `isPostHogConfigured() && getConsent() === 'granted'`. Impact: Low. Effort: Low. (`src/pages/Privacy.tsx:182`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** Privacy is the only SEO page with no page-level `structuredData` — add `buildWebPageSchema`. Impact: Low. Effort: Low. (`src/pages/Privacy.tsx:17-25`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [ ] **[Cleaner code]** Consent reset forces a full `window.location.reload()` to re-show the banner — could re-show reactively (acceptable as-is for a rare control). Impact: Low. Effort: Medium. (`src/pages/Privacy.tsx:9-13`) — added 2026-06-15 — **deferred 2026-06-15 (WP-J): kept the full reload — acceptable for a rarely-used control per plan; reactive rewrite is out of refinement scope**

### 12. 404 / Not Found

- [x] **[Accessibility]** CTAs/quick-links miss the 48px touch-target + `touch-manipulation` convention. Impact: Medium. Effort: Low. (`src/pages/NotFound.tsx:41-61,69-85`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Accessibility]** Decorative oversized `404` glyph is announced to screen readers — add `aria-hidden="true"`. Impact: Low. Effort: Low. (`src/pages/NotFound.tsx:18-28`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**
- [x] **[Consistency]** Buttons use `transition-colors` only — missing `active:scale-[0.98]` press feedback and the gold CTA glow. Impact: Low. Effort: Low. (`src/pages/NotFound.tsx:43,50,57`) — added 2026-06-15 — **completed 2026-06-15 (WP-E…J)**

### 13. Blog listing

- [x] **[Accessibility]** Blog filters expose no state, and the remove-chips announce as apply-buttons — The category row marks the active filter purely visually (a white pill) with no aria-pressed/aria-current, so a screen reader announces all six categories identically and a non-visual user cannot tell which one is applied. Impact: Medium. Effort: Low. (`src/pages/Blog.tsx:303`, `src/pages/Blog.tsx:308`, `src/pages/Blog.tsx:330`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Accessibility]** Each post card is two links to the same URL, one named by the entire card — The card's ViewTransitionLink wraps the image, the category/date/reading-time meta row, the h3 title AND the three-line excerpt, so its accessible name is the concatenation of all of it — a single link announcement running to roughly sixty words. Impact: Medium. Effort: Low. (`src/pages/Blog.tsx:447`, `src/pages/Blog.tsx:471`, `src/pages/Blog.tsx:489`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Accessibility]** Every interactive control on Blog is below the repo's own 44/48px tap-target rule — Blog.tsx contains no `min-h-[44px]`, no `min-h-[48px]` and no `touch-manipulation` anywhere. Computed heights: category pills `px-6 py-2 text-sm` ≈ 36px; active-filter chips `px-3 py-1 text-sm` ≈ 28px;. Impact: Medium. Effort: Low. (`src/pages/Blog.tsx:306`, `src/pages/Blog.tsx:332`, `src/pages/Blog.tsx:501`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** A Sanity outage blanks /blog and emits zero error events — the only capture path is provably dead — When the blog listing fetch fails or the payload fails shape validation, Blog.tsx calls `log.error(...)` and shows "Unable to load posts". That logger only ever reaches an error tracker through one gate in `emit()`: `sanitized?.error instanceof Error`. Impact: Medium. Effort: Low. (`src/pages/Blog.tsx:78`, `src/pages/Blog.tsx:86`, `src/pages/Blog.tsx:89`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** /blog preconnects the image CDN but not the API origin its render-blocking GROQ fetch actually uses — The /blog route declares `preconnects: ['https://cdn.sanity.io']`, which is the Sanity _asset_ CDN used by the card images. Impact: Medium. Effort: Low. (`src/routes.ts:198`, `src/sanity/client.ts:8`, `src/pages/Blog.tsx:74`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** A single drifted post rejects the entire listing, and the only offered remedy is a retry that can never succeed — `isBlogListingResult` requires every post in the array to pass `isSanityPostPreview`, so one document with a non-string `category`, `excerpt`, or `publishedAt` fails the whole result and Blog.tsx discards all posts and renders the error state. Impact: Medium. Effort: Medium. (`src/sanity/guards.ts:74`, `src/sanity/guards.ts:52`, `src/sanity/queries.ts:10`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Blog listing query pins no Sanity perspective and never excludes drafts on a public dataset — BLOG_LISTING_QUERY selects `*[_type == "post" && defined(slug.current)]` with no draft exclusion, and the browser client is created with no `perspective` option (only `apiVersion: '2024-01-01'`, `useCdn: true`), so which document versions the public site renders is decided by an unpinned Content Lake default. Impact: Medium. Effort: Low. (`src/sanity/queries.ts:5`, `src/sanity/client.ts:4`, `src/sanity/client.ts:7`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Accessibility]** Skeleton shimmer keeps sweeping under prefers-reduced-motion — BlogPostSkeleton applies `animate-shimmer` — a 2s linear infinite background sweep — to 11 bars per card, and the listing renders six skeletons, so ~66 elements animate continuously while the Sanity fetch is in flight. Impact: Low. Effort: Low. (`src/index.css:27`, `src/index.css:172`, `src/index.css:176`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** The post card is inlined in a 534-line page instead of extracted, against this repo's own card-component convention — Blog.tsx is 534 lines: SEO graph, hero, search form, category pills, active-filter chips, series header, a five-arm render ternary and a 78-line post card all in one component. Impact: Low. Effort: Medium. (`src/pages/Blog.tsx:442`, `src/pages/Blog.tsx:448`, `src/pages/BlogPost.tsx:567`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** BLOG_LISTING_QUERY fetches every tag and series document that nothing ever reads — BLOG_LISTING_QUERY is a three-part GROQ object: `posts`, plus a top-level `tags` array of every tag document and a top-level `series` array of every series document. Impact: Low. Effort: Medium. (`src/sanity/queries.ts:22`, `src/sanity/queries.ts:23`, `src/pages/Blog.tsx:83`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** Six hand-rolled URLSearchParams mutations in Blog.tsx have already drifted apart on history semantics — Every filter control rebuilds the query string with the same three-line block: clone `searchParams`, set/delete one key, call `setSearchParams`. It appears six times. Impact: Low. Effort: Low. (`src/pages/Blog.tsx:290`, `src/pages/Blog.tsx:373`, `src/pages/Blog.tsx:108`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** The listing fetch is the only page-level network call in src/ without an AbortController — fetchBlogData is a plain function redefined on every render, called from a `[]`-dep effect with no cleanup, and it passes no `signal` to client.fetch. Impact: Low. Effort: Low. (`src/pages/Blog.tsx:63`, `src/pages/Blog.tsx:74`, `src/pages/Blog.tsx:103`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Card hover fires a full-article fetch immediately, with no intent delay, no bound, and a silenced catch — `handleCardHover` runs on `mouseenter` of every card and immediately issues `POST_BY_SLUG_QUERY` — a query that projects the entire portable-text `body[]`, all `relatedPosts`, and a `seriesPosts` subquery, i.e. Impact: Low. Effort: Medium. (`src/pages/Blog.tsx:445`, `src/pages/Blog.tsx:170`, `src/pages/Blog.tsx:175`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Every rendered Sanity image fires a second CDN request for a placeholder Sanity already ships inline as a data URI — SanityResponsiveImage builds a separate cdn.sanity.io transform URL for a 20px blurred placeholder and applies it as a CSS `background-image` on the wrapper div. Impact: Low. Effort: Low. (`src/components/SanityResponsiveImage.tsx:47`, `src/components/SanityResponsiveImage.tsx:73`, `src/components/SanityResponsiveImage.tsx:86`) _(gap sweep)_ — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** Hover prefetch writes unvalidated posts into the shared post cache, bypassing the guard BlogPost depends on — The listing's hover prefetch fetches a full post and writes it straight into the shared postCache with only a truthiness check — no `isSanityPost` guard. BlogPost.tsx treats a cache hit as pre-validated and renders it directly, applying `isSanityPost` only on its own network path. Impact: Low. Effort: Low. (`src/pages/Blog.tsx:166`, `src/pages/Blog.tsx:170`, `src/pages/Blog.tsx:174`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** SanityResponsiveImage's tests pin only alt semantics — srcSet and the CLS intrinsic dimensions could be deleted and stay green — The component's two load-bearing behaviours for the blog listing are the multi-width `srcSet` and the `width`/`height` intrinsic attributes added specifically as a CLS fix (documented in a six-line comment). Impact: Low. Effort: Low. (`src/components/SanityResponsiveImage.tsx:42`, `src/components/SanityResponsiveImage.tsx:66`, `src/components/SanityResponsiveImage.tsx:82`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The N+1 "instrumented count" tests assert on a local stub and cannot fail no matter what Blog.tsx does — queries.noNPlusOne.test.ts advertises four ways of pinning the single-fetch contract, one of which is "an instrumented client sees exactly one fetch per view." No client is instrumented. Impact: Low. Effort: Low. (`src/sanity/queries.noNPlusOne.test.ts:18`, `src/sanity/queries.noNPlusOne.test.ts:31`, `src/sanity/queries.noNPlusOne.test.ts:106`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Hover prefetch writes unvalidated Sanity payloads into the post cache, bypassing the isSanityPost guard — The listing page validates everything it fetches before trusting it — `isBlogListingResult(result)` gates `setBlogListingCache` — but the card-hover prefetch does not: it takes whatever `client.fetch<SanityPost>` returns and writes it straight into the shared module-level post cache on a bare truthiness check. Impact: Low. Effort: Low. (`src/pages/Blog.tsx:171`, `src/pages/Blog.tsx:173`, `src/pages/Blog.tsx:77`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** JSON-LD is injected as raw HTML, so an unescaped '<' in a Sanity title can break out of the script tag in prerendered pages — The listing feeds Sanity-authored strings into JSON-LD — build-time post titles into the CollectionPage hasPart, and the series title/description plus post titles into the ItemList — and SEO.tsx serialises that with a bare `JSON.stringify` into a `<script type="application/ld+json">` child. Impact: Low. Effort: Low. (`src/components/SEO.tsx:188`, `src/pages/Blog.tsx:207`, `src/pages/Blog.tsx:214`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Listing image gate checks asset truthiness instead of the isRenderableImageSource guard, so one bad asset throws during render — The listing decides whether to render a post's cover with `post.image?.asset ?` — a truthiness check on an object, not a shape check. SanityResponsiveImage only guards `if (!source) return null` and then calls `urlFor(source)` immediately. Impact: Low. Effort: Low. (`src/pages/Blog.tsx:450`, `src/components/SanityResponsiveImage.tsx:32`, `src/sanity/guards.ts:47`) — added 2026-09-05 — **completed 2026-09-05**

### 14. Blog post reading

- [x] **[UI]** Every inline article image is hard-cropped to 4:3, cutting 28-65% off real published images — Portable Text `image` blocks are rendered into a wrapper pinned to `aspectRatio: '4 / 3'` with the image set to `object-cover`, and `SanityResponsiveImage` is asked to build 4:3 renditions. Impact: High. Effort: Medium. (`src/sanity/PortableTextComponents.tsx:79`, `src/sanity/PortableTextComponents.tsx:83`, `src/sanity/PortableTextComponents.tsx:86`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** Article body typography bypasses typography.ts, and the `prose` classes it leans on are inert — The article body is wrapped in `prose prose-invert prose-lg`, but `@tailwindcss/typography` is not a dependency and `src/index.css` contains only `@import 'tailwindcss'` with no `@plugin` directive, so none of those classes resolve to anything (and `not-prose` in the code block is dead too). Impact: Medium. Effort: Medium. (`src/pages/BlogPost.tsx:460`, `package.json:86`, `src/index.css:1`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** POST_BY_SLUG_QUERY never projects _updatedAt, so every post's dateModified silently collapses to publishedAt and contradicts sitemap lastmod — POST_BY_SLUG_QUERY is an explicit GROQ projection that lists 18 fields and does not include `_updatedAt`, so Sanity never returns it. Impact: Medium. Effort: Low. (`src/sanity/queries.ts:31`, `src/pages/BlogPost.tsx:330`, `src/pages/BlogPost.tsx:338`) _(gap sweep)_ — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** Both BlogPost failure paths are unreportable — the logger's error-capture branch is dead code — `BlogPost` has two failure states that render a dead-end page: `shape_validation_failed` (a CMS shape change) and `fetch_failed` (Sanity down, timeout, 5xx). Neither can reach an error tracker. Impact: Medium. Effort: Medium. (`src/pages/BlogPost.tsx:170`, `src/pages/BlogPost.tsx:183`, `src/utils/logger.ts:67`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** One code block downloads all 15 Shiki grammars (166 KB gz / 1.12 MB raw over 16 requests) — `createHighlighterInstance()` passes an array of 15 eagerly-evaluated `import()` calls for every supported language into `createHighlighterCore`, so the first `getHighlighter()` on a page fetches and compiles every grammar regardless of what the post actually uses. Impact: Medium. Effort: Medium. (`src/utils/shikiHighlighter.ts:30`, `src/utils/shikiHighlighter.ts:31`, `src/components/HighlightedCodeBlock.tsx:18`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Prerendered article is thrown away on mount, replaced by a skeleton, and re-fetched from Sanity — `scripts/prerender.js` writes a fully rendered article into `dist/blog/<slug>.html` — I confirmed `dist/blog/building-an-ai-chat-experience-for-your-personal-website.html` (49.8 KB) contains the real `<article class="py-12">` and its body. Impact: Medium. Effort: High. (`src/main.tsx:21`, `src/pages/BlogPost.tsx:134`, `src/pages/BlogPost.tsx:148`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** A build-time Sanity blip bakes "Article Not Found" + noindex into a real post's prerendered HTML, and nothing detects it — BlogPost renders `<SEO noindex>` on both of its failure branches, and SEO's effect unconditionally sets `window.__PRERENDER_READY__ = true`. Impact: Medium. Effort: Medium. (`src/pages/BlogPost.tsx:242`, `src/pages/BlogPost.tsx:278`, `src/components/SEO.tsx:130`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** The post loading skeleton doesn't match the page it stands in for, pulses through prefers-reduced-motion, and announces nothing — BlogPostArticleSkeleton draws a full-bleed 3:1 hero, then a max-w-3xl column pulled up with -mt-12, placing the title placeholder roughly 470px down a 1440px-wide viewport. Impact: Medium. Effort: Low. (`src/components/BlogPostArticleSkeleton.tsx:8`, `src/components/BlogPostArticleSkeleton.tsx:10`, `src/pages/BlogPost.tsx:355`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Accessibility]** Reading progress bar is exposed as a live progressbar, firing an update on every scroll frame — ReadingProgressBar renders a decorative 3px gold rule as role="progressbar" with aria-valuenow recomputed inside a requestAnimationFrame scroll handler, so aria-valuenow changes on essentially every frame a reader scrolls. Impact: Low. Effort: Low. (`src/components/ReadingProgressBar.tsx:45`, `src/components/ReadingProgressBar.tsx:46`, `src/components/ReadingProgressBar.tsx:21`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** `prose`/`not-prose` classes on the article body are dead — the Tailwind typography plugin is not installed — The article body is wrapped in `prose prose-invert prose-lg`, and HighlightedCodeBlock adds `not-prose` to opt its highlighted output out of those styles. Impact: Low. Effort: Low. (`src/pages/BlogPost.tsx:460`, `src/components/HighlightedCodeBlock.tsx:52`, `src/index.css:1`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** resolveInternalPath returns every non-http href as an "internal path", contradicting its own docblock — The docblock states that `mailto:` and anything that is not this site is external and returns `null`, but the implementation returns the raw href for anything not starting with `http`. Impact: Low. Effort: Low. (`src/sanity/PortableTextComponents.tsx:21`, `src/sanity/PortableTextComponents.tsx:30`, `src/sanity/PortableTextComponents.tsx:253`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** Shiki failures are swallowed by an empty catch and permanently memoized, unlike every other failure path in this feature — When the highlighter fails to load, the component catches with a comment and no logging, so a broken Shiki chunk degrades every code block on the blog to unstyled `<pre>` with zero signal. Impact: Low. Effort: Low. (`src/components/HighlightedCodeBlock.tsx:27`, `src/utils/shikiHighlighter.ts:55`, `src/pages/BlogPost.tsx:41`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Reading progress bar forces a layout read plus a React render on every scroll frame, and animates `width` — `updateProgress` reads `document.documentElement.scrollHeight` on every rAF tick and calls `setProgress` with an unrounded float, so React re-renders and rewrites the style on essentially every frame of every scroll. Impact: Low. Effort: Low. (`src/components/ReadingProgressBar.tsx:9`, `src/components/ReadingProgressBar.tsx:11`, `src/components/ReadingProgressBar.tsx:21`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** BlogPost's shape-drift and abort defences are unpinned: the integration mock never inspects its arguments and never returns a malformed post — The integration suite deliberately keeps the real `isSanityPost` so "the page's validation logic is exercised for real", but no test ever resolves a payload that fails it — grepping the file for "malformed" returns nothing — so the branch that logs `shape_validation_failed` and shows the malformed error has never executed in a test. Impact: Low. Effort: Medium. (`src/__tests__/integration/BlogPost.integration.test.tsx:9`, `src/__tests__/integration/BlogPost.integration.test.tsx:33`, `src/pages/BlogPost.tsx:168`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** HighlightedCodeBlock tests mock `shiki`, a module the component stopped importing — The test file stubs the bare `shiki` package's `codeToHtml`, but the component has since moved to the `getHighlighter()` singleton in `src/utils/shikiHighlighter.ts`, which dynamic-imports `shiki/core`, `shiki/engine/javascript` and 15 `@shikijs/langs/*` specifiers — none of which `vi.mock('shiki')` intercepts. Impact: Low. Effort: Low. (`src/components/HighlightedCodeBlock.test.tsx:6`, `src/components/HighlightedCodeBlock.tsx:2`, `src/utils/shikiHighlighter.ts:26`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** HighlightedCodeBlock's tests mock a module the component never imports, so the Shiki failure path is unverified — The test file mocks `'shiki'` and supplies a `codeToHtml`, but the component imports `getHighlighter` from `../utils/shikiHighlighter`, which dynamically imports `'shiki/core'`. Impact: Low. Effort: Low. (`src/components/HighlightedCodeBlock.test.tsx:6`, `src/components/HighlightedCodeBlock.tsx:2`, `src/utils/shikiHighlighter.ts:26`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** getHighlighter() memoizes a rejected promise for the life of the tab, silently disabling all syntax highlighting — `highlighterPromise` is assigned once and returned forever, with no `.catch` that resets it. Impact: Low. Effort: Low. (`src/utils/shikiHighlighter.ts:23`, `src/utils/shikiHighlighter.ts:54`, `src/components/HighlightedCodeBlock.tsx:19`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** CMS strings reach the prerendered JSON-LD as raw script text, so a `</script>` in a title or excerpt breaks out of the tag — BlogPost feeds CMS-authored strings (post.title, post.excerpt, post.category, tag titles) into buildArticleSchema, and SEO serialises the whole graph with a bare JSON.stringify into a `<script type="application/ld+json">` text node. Impact: Low. Effort: Low. (`src/components/SEO.tsx:188`, `src/pages/BlogPost.tsx:333`, `src/pages/BlogPost.tsx:341`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** resolveInternalPath classifies every non-http href as an internal route, so `//evil.com` renders as a trusted in-app link — resolveInternalPath returns the href unchanged whenever it does not start with 'http', which contradicts the function's own documented contract (`mailto:` is documented as external). Impact: Low. Effort: Low. (`src/sanity/PortableTextComponents.tsx:30`, `src/sanity/PortableTextComponents.tsx:21`, `src/sanity/PortableTextComponents.tsx:253`) — added 2026-09-05 — **completed 2026-09-05**

### 15. RSS feed

- [x] **[Resilience & tests]** The RSS/sitemap parity gate (VAL-SEO-009) runs before rss.xml exists, so it has never once checked the feed — `npm run build` runs `validate-prerender-seo.mjs` BEFORE `generate-sitemap.js` and `generate-rss.js`. Impact: Medium. Effort: Low. (`package.json:19`, `scripts/validate-prerender-seo.mjs:693`, `scripts/validate-prerender-seo.mjs:691`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** generate-rss.js is the one blog-data script outside the DRY pattern its sibling established — its own client, its own query, its own SITE_URL — generate-sitemap.js deliberately exports STATIC_ROUTES and BLOG_SLUGS_QUERY as a documented single source of truth, and prerender.js imports them so the crawl set can never drift. Impact: Low. Effort: Medium. (`scripts/generate-rss.js:15`, `scripts/generate-sitemap.js:17`, `scripts/prerender.js:34`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** The <category> projection is dead code — it queries a field no post has, and ships a blank line in every item — generate-rss.js projects `"categories": categories[]->title`, but the blog post schema has no `categories` array — it has a singular `category` reference (the frontend queries it as `coalesce(category->title, category)`). Impact: Low. Effort: Low. (`scripts/generate-rss.js:40`, `scripts/generate-rss.js:78`, `scripts/generate-rss.js:86`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The RSS/sitemap consistency gate can never fire — generate-rss.js runs after the validator that checks it, on a dist/ vite has just emptied — validate-prerender-seo.mjs implements VAL-SEO-009 / VAL-CROSS-005, a check that the set of blog slugs in dist/rss.xml equals the set in dist/sitemap.xml, guarded by `existsSync(rssFile) && existsSync(sitemapFile)`. Impact: Low. Effort: Low. (`package.json:19`, `scripts/validate-prerender-seo.mjs:691`, `scripts/validate-prerender-seo.mjs:693`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** escapeXml and formatRssDate can emit a feed no XML parser accepts, and nothing downstream checks well-formedness — `escapeXml` escapes the five XML entities but passes through characters that XML 1.0 forbids outright (U+0000-U+0008, U+000B, U+000C, U+000E-U+001F) — these cannot be represented in XML 1.0 at all, not even as numeric references. Impact: Low. Effort: Low. (`scripts/generate-rss.js:57`, `scripts/generate-rss.js:70`, `scripts/generate-rss.js:69`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** generate-rss.js has no test of any kind and re-declares the post query inline, so drift from the sitemap is unguarded — The RSS generator's pure functions — `escapeXml`, `formatRssDate`, `generateRssItem` — have zero test coverage; `scripts/` contains six `.test.mjs` files, all for `validate-*` scripts, none for a generator. Impact: Low. Effort: Medium. (`scripts/generate-rss.js:34`, `scripts/generate-sitemap.js:76`, `scripts/generate-sitemap.js:71`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** escapeXml lets XML-illegal control characters through, and formatRssDate can emit the literal string "Invalid Date" — escapeXml maps only the five predefined entities; it does not strip the C0 control characters that XML 1.0 forbids outright in document content. Impact: Low. Effort: Low. (`scripts/generate-rss.js:55`, `scripts/generate-rss.js:57`, `scripts/generate-rss.js:81`) — added 2026-09-05 — **completed 2026-09-05**

### 16. Alti chat (frontend)

- [x] **[Accessibility]** Sending a message disables the textarea, dropping focus to body and disarming the widget's focus trap and Escape key — handleSend sets isTyping synchronously, and both surfaces pass disabled={isTyping || isStreaming} straight through to the textarea element. Impact: High. Effort: Low. (`src/components/chat/ChatInput.tsx:82`, `src/pages/Chat.tsx:166`, `src/components/chat/ChatWidgetPanel.tsx:217`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The chat widget is the only unprotected subtree in the app — any throw inside it unmounts the whole React root to a blank page on 15 of 17 routes — `<ChatWidget />` is rendered at App.tsx:142 as a _sibling_ of the app's only ErrorBoundary (App.tsx:103), outside `<main>`, and main.tsx:26 renders `<App />` under StrictMode/LenisProvider/BrowserRouter/HelmetProvider with no boundary above it. Impact: High. Effort: Low. (`src/App.tsx:142`, `src/App.tsx:103`, `src/main.tsx:26`) _(gap sweep)_ — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Accessibility]** The /50 and /70 silver tints on chat instructional text and the composer placeholder fail WCAG AA contrast — The capability rail's subtitle and its closing instruction line, and the composer placeholder, are set in altivum-silver at 50 percent opacity on the altivum-dark ground. Impact: Medium. Effort: Low. (`src/components/chat/CapabilityIntro.tsx:147`, `src/components/chat/CapabilityIntro.tsx:89`, `src/components/chat/ChatInput.tsx:85`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Accessibility]** The whole thread is one aria-live log containing nested live regions, so streaming text is announced token-by-token and twice over — Both surfaces wrap the entire message list in role="log" aria-live="polite". Impact: Medium. Effort: Medium. (`src/pages/Chat.tsx:138`, `src/components/chat/ChatWidgetPanel.tsx:176`, `src/hooks/useChatEngine.ts:284`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** handleSend is a 275-line closure whose stream-drain block is duplicated verbatim — handleSend spans useChatEngine.ts:151-426 and carries request assembly, token fetch, transport, stream reading, parser draining, three state-reducer closures rebuilt on every send, empty-bubble cleanup and error-to-message mapping in one body. Impact: Medium. Effort: Medium. (`src/hooks/useChatEngine.ts:151`, `src/hooks/useChatEngine.ts:309`, `src/hooks/useChatEngine.ts:328`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Every streamed token re-serializes the whole transcript to sessionStorage and restarts a smooth scroll — The Lambda writes one stream chunk per model text delta, and the client calls setMessages once per parsed chunk. Impact: Medium. Effort: Medium. (`lambda/chat-stream/agent.mjs:163`, `src/hooks/useChatEngine.ts:315`, `src/hooks/useChatEngine.ts:284`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** The 3D mascot chunk + 1.1 MB GLB load on every page, for a 64px launcher nobody has clicked yet — ChatWidget is mounted on all 15 non-fullscreen routes, and ChatWidgetButton mounts AltiMascot immediately on first render — gated only on WebGL support and the prerender flag, never on intent. Impact: Medium. Effort: Low. (`src/App.tsx:142`, `src/components/chat/ChatWidgetButton.tsx:35`, `src/components/chat/ChatWidgetButton.tsx:52`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** A mid-stream abort silently truncates the answer, and the half-sentence is replayed to the model as history — Once the assistant bubble exists, both catch branches in handleSend are no-ops: they check `prev.some(m => m.id === assistantMessageId)` and return `prev` unchanged. Impact: Medium. Effort: Medium. (`src/hooks/useChatEngine.ts:158`, `src/hooks/useChatEngine.ts:380`, `src/hooks/useChatEngine.ts:396`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** Auto-scroll fires on every streamed token and force-scrolls the reader back to the bottom, ignoring reduced-motion — scrollToBottom is called from an effect keyed on the messages array, and applyText produces a new array on every decoded chunk, so a single response triggers dozens of smooth scrollTo calls. Impact: Medium. Effort: Low. (`src/hooks/useChatEngine.ts:142`, `src/hooks/useChatEngine.ts:148`, `src/hooks/useChatEngine.ts:284`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** Opening the widget on a cold chunk fetch flips aria-expanded to true while `Suspense fallback={null}` renders no dialog at all — Clicking the launcher sets `isOpen` (ChatWidget.tsx:15,22), which immediately flips `aria-expanded={isOpen}` to true on the button (ChatWidgetButton.tsx:48) and changes the tooltip to "Close chat" (ChatWidgetButton.tsx:41). Impact: Medium. Effort: Low. (`src/components/chat/ChatWidget.tsx:28`, `src/components/chat/ChatWidgetButton.tsx:48`, `src/components/chat/ChatWidgetPanel.tsx:3`) _(gap sweep)_ — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** Starter-chip defaults exist in two files, and the component-level copy is unreachable — The same four fallback starter prompts are declared twice: once in ChatSuggestions.tsx as the component's own default and once in pageContext.ts as the route-table fallback. Impact: Low. Effort: Low. (`src/components/chat/ChatSuggestions.tsx:8`, `src/components/chat/ChatSuggestions.tsx:16`, `src/utils/pageContext.ts:27`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** Every agent-driven navigation bypasses the site's view-transition wrapper — CLAUDE.md establishes useViewTransitionNavigate/ViewTransitionLink as the way this site navigates internally, and the chat surface is the only place in src that navigates programmatically at all. Impact: Low. Effort: Low. (`src/components/chat/ToolDraftCard.tsx:2`, `src/components/chat/ToolDraftCard.tsx:84`, `src/components/chat/ToolDraftCard.tsx:109`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** Forget-me is implemented twice, and the /chat page uses native window.confirm/window.alert — The same "forget me" operation is coded independently on the two surfaces that share the engine. ChatWidgetPanel wraps handleForgetMemory in a designed, dismissible in-panel status banner with role="status"/aria-live and success + failure styling;. Impact: Low. Effort: Medium. (`src/pages/Chat.tsx:46`, `src/pages/Chat.tsx:47`, `src/pages/Chat.tsx:53`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** ChatMessage re-linkifies the entire accumulated answer on every streamed token — The auto-link pass is memoized on `content`, which changes with every token of the streaming bubble, so processContentWithLinks re-scans the whole accumulated message from scratch on each update. Impact: Low. Effort: Low. (`src/components/chat/ChatMessage.tsx:190`, `src/components/chat/ChatMessage.tsx:191`, `src/components/chat/ChatMessage.tsx:136`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** Both parser flush tests assert an empty array while claiming to cover flush, leaving the one path that leaks raw NUL delimiters untested — createChatStreamParser.flush() exists to drain a buffer that push() deliberately held back, and by construction that leftover always contains a NUL byte — push() only retains the buffer when it sees an unterminated event frame or a partial delimiter. Impact: Low. Effort: Low. (`src/utils/chatEvents.test.ts:87`, `src/utils/chatEvents.test.ts:91`, `src/utils/chatEvents.test.ts:94`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The abort test is named for the timeout message but asserts nothing about it, and the 30s timer is never exercised anywhere — The only test covering abort semantics is titled "should show a timeout message when aborted before any chunks arrive", but its body never looks at the messages array — it asserts only that isStreaming and isTyping are false, with an in-file comment conceding "We only care that the hook did not crash". Impact: Low. Effort: Low. (`src/hooks/useChatEngine.test.ts:715`, `src/hooks/useChatEngine.test.ts:758`, `src/hooks/useChatEngine.test.ts:760`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** handleForgetMemory has no timeout, no abort signal and no in-flight guard, so a hung request leaves "Clearing your saved facts…" on screen forever — Every other network call in this feature is bounded — handleSend has an AbortController plus a 30s timer, the session-token manager has a circuit breaker — but the /forget call has neither a signal nor a timeout. Impact: Low. Effort: Low. (`src/hooks/useChatEngine.ts:439`, `src/hooks/useChatEngine.ts:428`, `src/components/chat/ChatWidgetPanel.tsx:69`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** A truncated stream emits the raw \x00EVT\x00 frame as assistant prose, which is persisted and replayed back into the next turn's prompt — `drainEvents` correctly holds a partial event in the buffer when the closing delimiter has not arrived (chatEvents.ts:129-131), but `flush()` then dumps that leftover buffer as `{kind:'text'}` (chatEvents.ts:174-175). Impact: Low. Effort: Medium. (`src/utils/chatEvents.ts:130`, `src/utils/chatEvents.ts:175`, `src/hooks/useChatEngine.ts:327`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** ToolDraftCard navigates and window.opens model-supplied targets with zero client-side validation, while its sibling renderer validates the same data — Draft-action cards act directly on strings that arrive over the chat stream: `navigate(action.path)` (ToolDraftCard.tsx:84), `navigate(`/blog/${action.slug}`)` (:166), and `window.open(action.url, '_blank', 'noopener,noreferrer')` (:197). Impact: Low. Effort: Low. (`src/components/chat/ToolDraftCard.tsx:84`, `src/components/chat/ToolDraftCard.tsx:166`, `src/components/chat/ToolDraftCard.tsx:197`) — added 2026-09-05 — **completed 2026-09-05**

### 17. Alti backend

- [x] **[Observability & cost]** Every chat failure returns HTTP 200, and none of the failure metrics has an alarm — Alti can be fully broken with zero signal — The handler's catch block converts every unexpected error into a 200 response with a \x00SYS\x00 message and returns normally, so AWS's built-in Lambda `Errors` metric stays at zero no matter what breaks. Impact: Medium. Effort: Low. (`lambda/chat-stream/index.mjs:445`, `lambda/chat-stream/index.mjs:449`, `lambda/chat-stream/index.mjs:439`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** The gen-UI Opus path records its own token metrics but sits outside the bedrock-cost alarm, which only watches the Haiku counter — Typing 'gen-ui' on /chat routes the turn to Claude Opus 4.6 via a forced-tool Converse call with maxTokens 1500 — roughly 15x the per-token cost of the Haiku agent it bypasses. Impact: Medium. Effort: Low. (`lambda/chat-stream/genUi.mjs:20`, `lambda/chat-stream/genUi.mjs:98`, `lambda/chat-stream/genUi.mjs:135`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The 25s agent timeout can never fire the AbortError branch — the SDK returns 'cancelled' gracefully, and the test mocks a throw that hides it — index.mjs arms a 25s AbortController and passes it to the Strands agent as `cancelSignal`, then relies on `catch (errName === "AbortError")` to record the `AgentTimeout` metric and tell the visitor "The response is taking too long." The installed SDK (@strands-agents/sdk 1.5.0) composes an external `cancelSignal` into the same abort controller `agent.cancel()` uses, catches the resulting `CancelledError` internally, and RETURNS an `AgentResult` with `stopReason: 'cancelled'` — it never throws out of the generator. Impact: Medium. Effort: Medium. (`lambda/chat-stream/index.mjs:356`, `lambda/chat-stream/index.mjs:420`, `lambda/chat-stream/node_modules/@strands-agents/sdk/dist/src/agent/agent.js:605`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The handler's error classifier is re-implemented inside the test instead of invoked, and the only handler-level test asserts nothing past the auth front door — streaming-contract.test.mjs's "index.mjs error classifier" test defines its own local `classify()` that duplicates index.mjs's catch-block branches, then asserts against that copy. Impact: Medium. Effort: Medium. (`lambda/chat-stream/__tests__/streaming-contract.test.mjs:353`, `lambda/chat-stream/__tests__/streaming-contract.test.mjs:364`, `lambda/chat-stream/index.mjs:420`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The per-request visitor-memory read is unbounded, while the write it mirrors is explicitly timeout-guarded — Every chat turn carrying a deviceId awaits `getFacts` before the model is invoked, with no timeout and no page cap. `getFacts` loops `while (collected.length < limit)` issuing paginated Queries until `LastEvaluatedKey` is absent;. Impact: Medium. Effort: Low. (`lambda/chat-stream/index.mjs:254`, `lambda/chat-stream/memory.mjs:68`, `lambda/chat-stream/memory.mjs:82`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** /forget is dispatched before the rate limiter, so the delete path is completely unmetered — The handler checks the rawPath for a "/forget" suffix and dispatches handleForget before entering the try block that performs the per-IP rate-limit check, then returns. Every other body-carrying request is capped at 20/hour per IP;. Impact: Medium. Effort: Low. (`lambda/chat-stream/index.mjs:184`, `lambda/chat-stream/index.mjs:194`, `lambda/chat-stream/memory.mjs:184`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Streaming guardrail runs in async mode and the intervention signal is discarded once text has streamed — buildBedrockModel configures the guardrail with `streamProcessingMode: "async"`. Per the Bedrock guardrails docs, async mode ships response chunks to the caller immediately and scans in the background, so "response chunks may contain inappropriate content until guardrails scan completes". Impact: Medium. Effort: Low. (`lambda/chat-stream/agent.mjs:50`, `lambda/chat-stream/index.mjs:386`, `lambda/chat-stream/agent.mjs:170`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** The gen-ui branch calls Opus with no guardrail and writes raw model text into the NUL-framed protocol — When a request's pageContext claims currentPage "/chat" and the text matches /\bgen[\s_-]?ui\b/i, index.mjs short-circuits into renderGenUi and never reaches the Strands agent. Impact: Medium. Effort: Medium. (`lambda/chat-stream/index.mjs:293`, `lambda/chat-stream/index.mjs:287`, `lambda/chat-stream/genUi.mjs:90`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** buildTools takes an untyped `any` bag; rememberFact declares a required timeoutMs dep the caller never passes, and nine re-exports are dead — `buildTools(deps)` is typed `@param {any} deps` and each tool re-destructures its own subset, while the registration gates in tools/index.mjs restate each tool's dependency list by hand. Impact: Low. Effort: Low. (`lambda/chat-stream/tools/index.mjs:13`, `lambda/chat-stream/tools/index.mjs:33`, `lambda/chat-stream/tools/index.mjs:40`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** Four tools hand-roll the same logger/try-catch/metrics block, and ToolCall_ is recorded before the work in three tools and after it in four — citePassage, searchBlog, searchPodcast and rememberFact each construct their own `createLogger(requestId, { service: "chat-stream" })`, wrap the body in a near-identical try/catch, log the same `tool_error` shape, and emit `ToolCall_X` / `ToolFailure_X` / `ToolLatency_X` by hand. Impact: Low. Effort: Medium. (`lambda/chat-stream/tools/citePassage.mjs:31`, `lambda/chat-stream/tools/searchBlog.mjs:41`, `lambda/chat-stream/tools/searchPodcast.mjs:82`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** Chat model ID and region are hardcoded while every sibling Lambda (and this file's own gen-ui path) reads them env-first — index.mjs pins the chat model as a bare literal (`const MODEL_ID = "us.anthropic.claude-haiku-4-5-20251001-v1:0"`, line 48) and repeats the literal region "us-east-1" in four SDK client constructors plus the Bedrock model config. Impact: Low. Effort: Low. (`lambda/chat-stream/index.mjs:48`, `lambda/chat-stream/index.mjs:49`, `lambda/chat-stream/index.mjs:30`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** TotalRequestLatency and the ChatMessageSent product event fire on only two of the handler's eleven exit paths — Both end-of-request telemetry signals are emitted only on the success path and the unhandled-error path. Impact: Low. Effort: Low. (`lambda/chat-stream/index.mjs:407`, `lambda/chat-stream/index.mjs:446`, `lambda/chat-stream/index.mjs:208`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** stopReason is threaded all the way out of the stream and then discarded — truncated and cancelled answers are logged as clean successes — `streamAgentResponse` returns `stopReason` from the terminal AgentResult, but index.mjs reads it in exactly one place: inside the `!result.hadText` branch. Impact: Low. Effort: Low. (`lambda/chat-stream/agent.mjs:98`, `lambda/chat-stream/agent.mjs:205`, `lambda/chat-stream/index.mjs:400`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** POST /forget short-circuits before the rate limiter and trusts a body-supplied deviceId instead of the token's own deviceHash — The handler routes any path ending in `/forget` to `handleForget` and returns, several lines before `checkRateLimit` is ever called. Impact: Low. Effort: Low. (`lambda/chat-stream/index.mjs:184`, `lambda/chat-stream/index.mjs:194`, `lambda/chat-stream/index.mjs:116`) — added 2026-09-05 — **completed 2026-09-05**

### 18. Alti 3D mascot

- [x] **[Performance]** The mascot's ~250 KB of JS plus a 1.1 MB GLB download on first paint of every page, for a decoration most visitors never click — ChatWidget is rendered on every non-fullscreen route (App.tsx:142), ChatWidgetButton is a static import inside it (ChatWidget.tsx:32), and when WebGL is available the lazy AltiMascot element is rendered immediately and unconditionally (ChatWidgetButton.tsx:35, :52-54). Impact: Medium. Effort: Medium. (`src/App.tsx:142`, `src/components/chat/ChatWidget.tsx:32`, `src/components/chat/ChatWidgetButton.tsx:35`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** A lost WebGL context leaves the launcher visually empty with no route back to MascotFallback — The mascot's only visual is the 64x64 R3F Canvas; the button contains nothing else but a small gold ellipse. `checkWebGLSupport()` is consulted once, at first render, and its result is cached for the page lifetime, so the fallback is only reachable if WebGL was never available. Impact: Medium. Effort: Low. (`src/components/chat/AltiMascot.tsx:86`, `src/components/chat/AltiMascot.tsx:85`, `src/components/chat/AltiMascot.tsx:101`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** AltiMascot has no test file, so the reduced-motion and tab-visibility frameloop contract is entirely unpinned — AltiMascot.tsx carries the feature's whole motion-accessibility and battery contract in one unverified expression: `frameloop` is 'demand' under prefers-reduced-motion, 'always' when visible, 'never' when the tab is hidden, plus a `!animate` early-return inside useFrame and a visibilitychange listener. Impact: Medium. Effort: Medium. (`src/components/chat/AltiMascot.tsx:81`, `src/components/chat/AltiMascot.tsx:16`, `src/components/chat/AltiMascot.tsx:71`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** AltiMascot owns the launcher's plain-DOM chrome, so the non-WebGL branch renders a different button — AltiMascot is not just the 3D mascot: it also renders the launcher's non-WebGL chrome — the flex column, the gold platform ellipse with its hover/idle glow, and the `close` glyph that appears when the panel is open. Impact: Low. Effort: Medium. (`src/components/chat/AltiMascot.tsx:65`, `src/components/chat/AltiMascot.tsx:84`, `src/components/chat/AltiMascot.tsx:101`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** SafeCanvas passes an inert showHomeButton and omits the pageName every other custom-fallback boundary sets — SafeCanvas passes `showHomeButton={false}` to ErrorBoundary, but it always supplies a `fallback` prop (defaulted to `null` on the same line), and ErrorBoundary short-circuits to the fallback whenever `'fallback' in this.props`. Impact: Low. Effort: Low. (`src/components/SafeCanvas.tsx:26`, `src/components/SafeCanvas.tsx:27`, `src/components/ErrorBoundary.tsx:69`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** The tab-visibility frameloop gate is copy-pasted verbatim between AltiMascot and TopologyScene — AltiMascot and TopologyScene contain a byte-for-byte identical `docVisible` state plus `visibilitychange` effect to pause the rAF loop when the tab is hidden. Impact: Low. Effort: Low. (`src/components/chat/AltiMascot.tsx:70`, `src/components/aws/TopologyScene.tsx:285`, `src/components/aws/TopologyScene.tsx:283`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** The bundle-size budget guard cannot see alti.glb, the single largest byte cost this feature ships — scripts/track-bundle-size.mjs measures only dist/assets and only .js/.css files (lines 29 and 64), and the budgets file keys everything off chunk-name prefixes (bundle-size-budgets.json:5-13). Impact: Low. Effort: Low. (`scripts/track-bundle-size.mjs:29`, `scripts/track-bundle-size.mjs:64`, `bundle-size-budgets.json:11`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** checkWebGLSupport leaks its probe WebGL context for the lifetime of the document — checkWebGL.ts creates a throwaway canvas, acquires a real webgl2 (or webgl) context to test support, caches the boolean, and returns — it never calls WEBGL_lose_context.loseContext(). Impact: Low. Effort: Low. (`src/utils/checkWebGL.ts:7`, `src/utils/checkWebGL.ts:8`, `src/utils/checkWebGL.ts:9`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** checkWebGLSupport leaks its probe context and latches its answer for the page lifetime, untested — The probe creates a real WebGL context to test for support and then drops the canvas without releasing it — no `WEBGL_lose_context.loseContext()` — so a live GPU context is held until GC for a check that needed it for one statement. Impact: Low. Effort: Low. (`src/utils/checkWebGL.ts:8`, `src/utils/checkWebGL.ts:9`, `src/utils/checkWebGL.ts:4`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UI]** Launcher jumps 10px upward when the lazy 3D chunk swaps in over the fallback — `MascotFallback` is a flat 64x64 box, but `AltiMascot` renders a 64px canvas stacked above a 16px platform pulled up 6px — a 74px-tall column. The launcher is `fixed bottom-6` with no fixed height, so its bottom edge is anchored and the box grows upward. Impact: Low. Effort: Low. (`src/components/chat/ChatWidgetButton.tsx:19`, `src/components/chat/ChatWidgetButton.tsx:44`, `src/components/chat/ChatWidgetButton.tsx:7`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UI]** Open/closed state is invisible on the launcher: the fallback ignores isOpen, and the 3D path's cue is a 10px glyph — `MascotFallback` accepts no props and always renders the same `support_agent` icon, and it is the launcher's entire visual on WebGL-blocklisted GPUs, during the lazy three/drei chunk's Suspense, and after a 3D mount failure. Impact: Low. Effort: Low. (`src/components/chat/ChatWidgetButton.tsx:16`, `src/components/chat/ChatWidgetButton.tsx:24`, `src/components/chat/ChatWidgetButton.tsx:57`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** Hover feedback is scoped to the mesh raycast, and the launcher has no press state at all — The gold platform glow — the launcher's only hover affordance — is driven by R3F `onPointerEnter`/`onPointerLeave` on the model `<group>`, i.e. a raycast hit against the GLB mesh. Impact: Low. Effort: Low. (`src/components/chat/AltiMascot.tsx:45`, `src/components/chat/AltiMascot.tsx:50`, `src/components/chat/AltiMascot.tsx:105`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** Launcher tooltip pins itself open after every chat close (focus-within, not focus-visible) — The tooltip reveals on `group-focus-within:opacity-100`, which matches _any_ focus — mouse and programmatic included — while the button's own indicator uses `focus-visible:ring-2`. Impact: Low. Effort: Low. (`src/components/chat/ChatWidgetButton.tsx:63`, `src/components/chat/ChatWidgetButton.tsx:39`, `src/components/chat/ChatWidgetButton.tsx:50`) — added 2026-09-05 — **completed 2026-09-05**

### 19. Visitor memory

- [x] **[Observability & cost]** Every visitor-memory failure signal is emitted but unalarmed — memory can be 100% dead in prod and nothing tells anyone — The memory feature emits failure metrics on all three of its paths — `MemoryLoadFailure` on read, `ForgetFailure` on erase, `ToolFailure_RememberFact`/`ToolTimeout_RememberFact` on write — but no alarm anywhere watches any of them. Impact: Medium. Effort: Low. (`lambda/chat-stream/index.mjs:258`, `lambda/chat-stream/index.mjs:251`, `lambda/chat-stream/index.mjs:134`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The forget-me fetch has no timeout, so a stalled erase leaves the widget permanently "Clearing…" — `handleForgetMemory` issues its POST /forget with no AbortController and no timeout, unlike `handleSend` on the same hook which aborts after 30s. The widget sets "Clearing your saved facts…" and awaits, with nothing that can ever clear that banner if the request stalls;. Impact: Medium. Effort: Low. (`src/hooks/useChatEngine.ts:439`, `src/hooks/useChatEngine.ts:158`, `src/hooks/useChatEngine.ts:222`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** POST /forget is dispatched above the rate limiter, so memory erasure is unthrottled — The handler dispatches `/forget` and returns at line 186, several lines before `checkRateLimit` runs at line 194 — and `handleForget` contains no throttle of its own. Every other request path is capped at 20/hr per IP;. Impact: Medium. Effort: Low. (`lambda/chat-stream/index.mjs:184`, `lambda/chat-stream/index.mjs:194`, `lambda/chat-stream/index.mjs:112`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** hashDeviceId is copy-pasted into three Lambdas that must agree byte-for-byte, with no shared module or drift test — `memory.mjs` defines `hashDeviceId` as sha256-hex of the raw device id and uses it as the memory partition key. Impact: Low. Effort: Medium. (`lambda/chat-stream/memory.mjs:29`, `lambda/session-token/index.mjs:68`, `lambda/session-token/index.mjs:170`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** memory.schema.mjs is orphaned from the shared schema registry it types itself against — `memory.schema.mjs` declares itself as an instance of `DynamoDbTableSchema`, a typedef owned by `lambda/shared/dynamoDbSchemas.mjs`, but it reaches for it with a relative `../shared/...` path rather than the `lambda-shared/...` specifier every other cross-package import in these Lambdas uses — because `dynamoDbSchemas.mjs` is in neither the shared barrel nor the package's exports map. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.schema.mjs:26`, `lambda/shared/dynamoDbSchemas.mjs:5`, `lambda/shared/dynamoDbSchemas.mjs:66`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** The email and phone PII regexes are maintained twice, in memory.mjs and lambda-shared/logger.mjs — `memory.mjs` defines `EMAIL_PATTERN` and `PHONE_PATTERN` to reject PII before persistence. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:21`, `lambda/chat-stream/memory.mjs:26`, `lambda/shared/logger.mjs:28`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** PII rejection is thrown as a generic Error, so a working privacy control reports as ToolFailure_RememberFact — `sanitizeFactContent` collapses four distinct outcomes — empty-after-trim, prompt-injection sentinel, email, phone-shaped digit run — into a single `""` return, and `putFact` turns all of them into one untyped `throw new Error("putFact: content is empty or rejected after sanitization")`. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:41`, `lambda/chat-stream/memory.mjs:112`, `lambda/chat-stream/tools/rememberFact.mjs:60`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** PII rejection is counted and logged as a tool failure, so the metric you would alarm on is pre-poisoned and the PII guard itself is invisible — `putFact` throws on sanitization rejection, which is the normal, working-as-designed outcome whenever the model tries to remember an email or phone-shaped digit run. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:112`, `lambda/chat-stream/memory.mjs:42`, `lambda/chat-stream/tools/rememberFact.mjs:61`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** getFacts is the only unbounded dependency on the pre-stream critical path — a hung Query burns the full 60s Lambda timeout — `putFact` is deliberately bounded at 4s via `withTimeout`, with a comment explaining that a hung DynamoDB write must not starve the turn, and KB retrieval next door is bounded at 4s too. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:60`, `lambda/chat-stream/memory.mjs:120`, `lambda/chat-stream/memory.mjs:5`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** forgetDevice throws away a partial erase when BatchWrite retries are exhausted — `flushBatch` throws once `UnprocessedItems` survives BATCH_RETRY_MAX retries, and that throw unwinds the whole `forgetDevice` page loop. Impact: Low. Effort: Medium. (`lambda/chat-stream/memory.mjs:158`, `lambda/chat-stream/memory.mjs:203`, `lambda/chat-stream/index.mjs:123`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** getFacts is the only unbounded dependency call on the chat critical path — Every chat turn for a returning visitor awaits `getFacts` before the agent starts, and `getFacts` accepts no timeout and caps nothing but the fact count — its paging loop keeps issuing Queries until `LastEvaluatedKey` is absent. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:60`, `lambda/chat-stream/memory.mjs:68`, `lambda/chat-stream/index.mjs:254`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** putFact writes duplicates unconditionally, so repeat saves silently evict a visitor's real memory — putFact builds factId as `${paddedTimestamp}#${randomUUID()}` and issues a bare PutCommand with no ConditionExpression, so the same sanitized text saved twice produces two distinct rows that can never collide. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:116`, `lambda/chat-stream/memory.mjs:122`, `lambda/chat-stream/memory.mjs:10`) _(gap sweep)_ — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** putFact's timeout races instead of cancelling, and the shipped timeout value is never tested — `putFact` bounds the DynamoDB write with `withTimeout`, a `Promise.race` whose own docblock says the underlying work is NOT cancelled and that DynamoDB calls should use an abortSignal instead. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:120`, `lambda/shared/timeout.mjs:6`, `lambda/shared/timeout.mjs:11`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Forget-me clears the device id but not the cached session token that signs it — `handleForgetMemory` erases the visitor's localStorage device id and transcript, but leaves the module-level session-token cache untouched, so the browser keeps sending a server-signed token whose payload is sha256 of the ERASED device id on every subsequent chat request for the rest of its 30-minute TTL. Impact: Low. Effort: Low. (`src/hooks/useChatEngine.ts:428`, `src/hooks/useChatEngine.ts:459`, `src/hooks/useChatEngine.ts:431`) _(gap sweep)_ — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Memory partition is picked by the request body, ignoring the signed deviceHash already in the session token — Every visitor-memory operation partitions on `body.deviceId`, which the client supplies and the server only format-checks (`/^[a-zA-Z0-9_-]{8,64}$/`). Impact: Low. Effort: Medium. (`lambda/chat-stream/index.mjs:224`, `lambda/chat-stream/index.mjs:116`, `lambda/chat-stream/index.mjs:165`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Stored facts are re-injected into the system prompt without re-running the sanitiser that gates writes — `sanitizeFactContent` is a write-time-only control: `putFact` calls it, but `getFacts` returns `item.content` verbatim and `buildMemoryContext` interpolates each value straight into the system prompt. Impact: Low. Effort: Low. (`lambda/chat-stream/memory.mjs:37`, `lambda/chat-stream/memory.mjs:111`, `lambda/chat-stream/memory.mjs:83`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** The raw device id — the key to a visitor's memory partition — is shipped to Sentry and CloudWatch RUM under comments claiming it is a SHA-256 hash — `App.tsx` reads `getOrCreateDeviceId()` and forwards the raw value straight into both RUM and Sentry as the user identifier. Impact: Low. Effort: Low. (`src/App.tsx:70`, `src/App.tsx:67`, `src/utils/deviceId.ts:31`) — added 2026-09-05 — **completed 2026-09-05**

### 20. Knowledge Base + sync

- [x] **[Architecture & maintainability]** Three hand-copied Bedrock Retrieve implementations that have already drifted apart — The exact same Retrieve routine — build RetrieveCommand with retrievalQuery/vectorSearchConfiguration, wrap in an AbortController timeout, time it, record latency, map result.content.text, swallow every error — exists in three places: kbRetrieve.mjs, podcastRetrieve.mjs, and the mcp-server's private retrieveKbContext. Impact: Medium. Effort: Medium. (`lambda/chat-stream/kbRetrieve.mjs:26`, `lambda/chat-stream/podcastRetrieve.mjs:37`, `lambda/mcp-server/tools/askAlti.mjs:49`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** A knowledge base returning zero hits reports as 100% healthy on the admin dashboard — retrieveContext's empty-results branch returns null after recording only KBRetrievalLatency — neither KBRetrievalSuccess nor KBRetrievalFailure. The admin health endpoint derives kbSuccessRate as successes/(successes+failures), so empty retrievals are excluded from both numerator and denominator. Impact: Medium. Effort: Low. (`lambda/chat-stream/kbRetrieve.mjs:44`, `lambda/chat-stream/kbRetrieve.mjs:45`, `lambda/chat-stream/kbRetrieve.mjs:53`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** kb-sync alarms on "ingestion job accepted", never on "ingestion job succeeded" — The handler records KBSyncTriggered and returns the instant StartIngestionJob is accepted, and logs response.ingestionJob.status — which at that moment is always STARTING. Impact: Medium. Effort: Medium. (`lambda/kb-sync/index.mjs:62`, `lambda/kb-sync/index.mjs:65`, `lambda/kb-sync/index.mjs:71`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The "LIVE" KB contract test is a tautology — it passes with no AWS credentials, a wrong KB id, or a wrong region — kb-retrieve-contract.test.mjs exists specifically (per its own header) to prove the live KB is reachable, that the configured KB id is valid, and that the response shape still parses. Impact: Medium. Effort: Low. (`lambda/chat-stream/__tests__/kb-retrieve-contract.test.mjs:62`, `lambda/chat-stream/kbRetrieve.mjs:42`, `lambda/chat-stream/kbRetrieve.mjs:59`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** kb-sync permanently drops retryable ConflictException/ThrottlingException, leaving the KB silently stale after a second publish — The handler catches every StartIngestionJob error, records KBSyncFailure, and resolves with a 500 body that nothing reads — the comment says this is deliberate so "S3 won't retry on transient errors." But Bedrock's quota is one ingestion job per data source and StartIngestionJob is rate-limited to 0.1 rps, so the two most likely errors here (ConflictException when a job is already running, ThrottlingException on rapid publishes) are precisely the transient, retryable ones. Impact: Medium. Effort: Medium. (`lambda/kb-sync/index.mjs:95`, `lambda/kb-sync/index.mjs:96`, `lambda/kb-sync/index.mjs:62`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** kb-sync's test locks in the pre-migration KB ids and its header comment claims an env override that now exists — kb-sync/index.mjs became env-first in commit 2647a8e (KB_ID / DATA_SOURCE_ID with the current values as fallback), but its test was not updated. The test file's header still tells the next reader "These IDs are hard-coded as module constants in index.mjs. Impact: Medium. Effort: Low. (`lambda/kb-sync/index.mjs:15`, `lambda/kb-sync/index.mjs:16`, `lambda/kb-sync/__tests__/index.test.mjs:30`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** retrieveContext can return an empty string while its JSDoc, and its own contract test, say it returns null — The emptiness check at line 44 tests the RAW retrievalResults, but the chunks are filtered for content.text afterwards. A response whose results all lack content.text therefore skips the null return, produces "" from join(), records KBRetrievalSuccess, and logs kb_retrieval_success with chunks: 0. Impact: Low. Effort: Low. (`lambda/chat-stream/kbRetrieve.mjs:15`, `lambda/chat-stream/kbRetrieve.mjs:44`, `lambda/chat-stream/kbRetrieve.mjs:49`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** kbRetrieve clears its abort timer only on the success path, unlike every other timeout in the repo — retrieveContext arms a setTimeout at line 24 and clears it at line 40 — inside the try, after the await resolves. Any rejection from agentClient.send (ThrottlingException, ValidationException, AccessDeniedException, a socket error) jumps straight to the catch at line 56, which never clears the timer. Impact: Low. Effort: Low. (`lambda/chat-stream/kbRetrieve.mjs:24`, `lambda/chat-stream/kbRetrieve.mjs:40`, `lambda/chat-stream/kbRetrieve.mjs:56`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** kb-sync wraps an abortable Bedrock call in withTimeout, against the shared module's own guidance — kb-sync bounds StartIngestionJob with withTimeout, which is a Promise.race that does not cancel the underlying request — the shared module's own doc comment explicitly says to pass an AbortController for SDK calls that accept one, naming Bedrock. Impact: Low. Effort: Low. (`lambda/kb-sync/index.mjs:62`, `lambda/shared/timeout.mjs:5`, `lambda/shared/timeout.mjs:10`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Visitor memory and KB retrieval run serially before the first token streams — The chat handler awaits getFacts to completion, then awaits retrieveContext, before it can build the system prompt and start the agent. Impact: Low. Effort: Low. (`lambda/chat-stream/index.mjs:254`, `lambda/chat-stream/index.mjs:273`, `lambda/chat-stream/index.mjs:268`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** kb-sync test pins hard-coded KB ids behind a comment that is now false, leaving the migration's env-first path untested — Commit 2647a8e made kb-sync read KB_ID and DATA_SOURCE_ID from the environment (with the current prod values as defaults) to prepare the migration to account 512880383078. Impact: Low. Effort: Low. (`lambda/kb-sync/index.mjs:15`, `lambda/kb-sync/index.mjs:16`, `lambda/kb-sync/__tests__/index.test.mjs:30`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** retrieveContext reports KBRetrievalSuccess when zero results were parseable, so a response-shape change makes Alti silently ungrounded with no alarm — The empty guard at line 44 only checks whether `retrievalResults` is missing or length 0. Impact: Low. Effort: Low. (`lambda/chat-stream/kbRetrieve.mjs:44`, `lambda/chat-stream/kbRetrieve.mjs:49`, `lambda/chat-stream/kbRetrieve.mjs:53`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** Retrieved KB passages are spliced into the system prompt with the block's own delimiters intact — retrieveContext() takes each Bedrock result's content.text verbatim and joins them into one string, and buildSystemPrompt() drops that string between the literal markers "=== RETRIEVED CONTEXT ===" and "=== END CONTEXT ===". Impact: Low. Effort: Low. (`lambda/chat-stream/kbRetrieve.mjs:51`, `lambda/chat-stream/kbRetrieve.mjs:55`, `lambda/chat-stream/prompts.mjs:159`) — added 2026-09-05 — **completed 2026-09-05**

### 29. SEO

_Medium impact (correctness / authority defects):_

- [x] **[Resilience & tests]** Podcast featured-episode `VideoObject` ships without Google's REQUIRED `uploadDate` — `buildVideoObjectSchema` makes it optional and Podcast.tsx never passes it (the ISO date `LATEST_EPISODE_DATE` is already imported), so the episode is ineligible for video rich results; the blog path passes it correctly. Wire it through + make the builder param required. Impact: Medium. Effort: Low. (`src/utils/schemas.ts:386-397`, `src/pages/Podcast.tsx:8,75-79`) — added 2026-06-20 — **completed 2026-06-20: uploadDate now a required builder param + passed from `LATEST_EPISODE_DATE` (guarded); covered by schemas.test.ts**
- [x] **[Structured data]** `@id` node collisions — the default-graph Person & Organization nodes are re-declared with DIVERGENT content by Altivum.tsx (a 2nd `altivum.ai/#organization`) and by every blog post (a partial Person `author` re-using `thechrisgrey.com/#person`), so two nodes share one `@id` in a single `@graph` and the per-post tag `knowsAbout` can leak onto the global Person identity. Emit bare `{ "@id": ... }` references instead; add a graph-uniqueness test. Impact: Medium. Effort: Medium. (`src/components/SEO.tsx:82-84`, `src/pages/Altivum.tsx:139-187`, `src/pages/BlogPost.tsx:305-311`) — added 2026-06-20 — **completed 2026-06-20: inline Org node removed from Altivum (canonical default node is a superset; dropped `vector.altivum.ai`), BlogPost author → bare `@id` ref; top-level `@graph` uniqueness test added to SEO.test.tsx**
- [x] **[Structured data]** `VideoObject.thumbnailUrl` (a REQUIRED field) defaults to `maxresdefault.jpg`, which 404s for SD/third-party videos; both callers rely on the default (the repo's own YouTubeFacade already falls back maxres→hq). Default to `hqdefault.jpg` (universally generated). Impact: Medium. Effort: Low. (`src/utils/schemas.ts:390`) — added 2026-06-20 — **completed 2026-06-20**
- [x] **[Security]** `/admin` renders NO `<SEO>` and is served as the raw SPA shell carrying index.html's `index, follow` — the `noIndex: true` route flag only drives sitemap/prerender exclusion, not a runtime robots tag (Blueprint handles the same case correctly). Render `<SEO noindex url=".../admin">` (mirror Blueprint) and add `Disallow: /admin` to robots.txt for a pre-JS guarantee. (404 status currently mitigates, so this is defense-in-depth.) Impact: Medium. Effort: Low. (`src/pages/Admin.tsx`, `src/routes.ts:234`, `index.html:21`, `src/pages/Blueprint.tsx:138`) — added 2026-06-20 — **completed 2026-06-20: `<SEO noindex>` now renders for all admin states; chose noindex-only (a robots.txt Disallow would block the noindex from being read, and the route is already 404-served)**
- [x] **[Resilience & tests]** JSON-LD is never structurally validated — every SEO test is a `toContain` substring check; no test asserts schema.org REQUIRED fields, so a builder can drop a required property (e.g. VideoObject `uploadDate`, Book `Offer.price`) and ship green (the project's own documented failure mode). Add per-`@type` required-field assertions in schemas.test.ts. Impact: Medium. Effort: Medium. (`src/utils/schemas.test.ts:202-216`, `src/components/SEO.test.tsx:90-94`) — added 2026-06-20 — **completed 2026-06-20: `REQUIRED_FIELDS_BY_TYPE` harness asserts every builder's required fields + a parseable-`@graph` assertion in SEO.test.tsx**
- [x] **[Resilience & tests]** No build-time gate validates the JSON-LD / canonical / OG of the actually-prerendered dist HTML (the file Google indexes) — only `isPrerender()` is tested. Extend the existing ogCards drift-test idiom to parse each prerendered `<head>`; scope as opt-in/STRICT to respect prerender's deliberately non-fatal design. (Repo was just burned by a prerender/canonical bug, #170.) Impact: Medium. Effort: Medium. (`src/utils/prerender.test.ts`, `scripts/prerender.js`, `src/utils/ogCards.test.ts:42-54`) — added 2026-06-20 — **completed 2026-06-20: `scripts/validate-prerender-seo.mjs` added to the build chain after prerender; non-fatal by default, fails under `STRICT_PRERENDER`; verified against the real `dist/` (JSON-LD + canonical clean on all 13 routes)**
- [x] **[Consistency]** Organization `sameAs` LinkedIn URL is a dead 404 — `/company/altivum-inc` vs the canonical `/company/altivuminc` (200) already in `SOCIAL_LINKS`; a 404 `sameAs` ships site-wide in the default graph and weakens entity grounding. Fix the hyphen / source from `SOCIAL_LINKS` (mirror the #169 X-handle reconcile). Impact: Medium. Effort: Low. (`src/utils/schemas.ts:168`, `src/constants/links.ts:22`) — added 2026-06-20 — **completed 2026-06-20: Org `sameAs` now sourced from `SOCIAL_LINKS` (`.altivumLinkedIn`/`.github`/`.altivumLogic`); regression test added**

_Low impact (standards completeness, hygiene, tests, GEO/E-E-A-T):_

- [ ] **[Structured data]** Person `image` is the 1200×630 social card (`og.png`), not a clean portrait — point at `profile1.jpeg` as an `ImageObject` (keep og.png for og:image). Impact: Low. Effort: Low. (`src/utils/schemas.ts:37`) — added 2026-06-20
- [ ] **[Consistency]** Person `sameAs` diverges from `SOCIAL_LINKS` — omits the live Instagram profile and includes `logic.altivum.ai` (a service site, not a personal identity URL). Add IG, drop the service, derive from the constant. Impact: Low. Effort: Low. (`src/utils/schemas.ts:99-109`, `src/constants/links.ts:10`) — added 2026-06-20
- [ ] **[Content]** Two podcast FAQ answers don't lead with a citable fact (release cadence vague; guest answer opens with marketing copy) — weak GEO/answer-engine extraction. Rewrite first sentences to be standalone-citable (use the real cadence; don't fabricate). Impact: Low. Effort: Low. (`src/utils/schemas.ts:482-487`) — added 2026-06-20
- [x] **[Cleaner code]** CSR-served noindex routes carry a conflicting double robots directive (shell `index,follow` + helmet `noindex,nofollow`) — make robots a single always-emitted tag in SEO.tsx and remove the hardcoded one from index.html. Impact: Low. Effort: Low. (`index.html:21`, `src/components/SEO.tsx:111`) — added 2026-06-20 — **completed 2026-09-05** (index.html no longer hardcodes a robots meta; SEO.tsx is the only emitter)
- [x] **[Accessibility]** OG/Twitter cards have no `og:image:alt` / `twitter:image:alt` — add an optional `imageAlt` prop defaulting to `fullTitle`/post title. Impact: Low. Effort: Low. (`src/components/SEO.tsx:121-126,144`) — added 2026-06-20 — **completed 2026-09-05** (og:image:alt + twitter:image:alt now emitted)
- [ ] **[UX]** `/blueprint` flag-on state is unhandled for SEO — the enabled `<SEO>` variant omits `noindex` AND the route is never prerendered (served as a 404 generic Home shell pre-JS). Decide indexability: noindex both branches, or add to `STATIC_ROUTES` gated on the flag so prerender writes a 200 with the right canonical. Impact: Low. Effort: Medium. (`src/pages/Blueprint.tsx:138,152-162`, `scripts/generate-sitemap.js:29-43`) — added 2026-06-20
- [ ] **[Cleaner code]** `<meta name="keywords">` is emitted on ~13 pages — ignored by all engines since 2009; drop the emission, the prop, and the per-page strings (keep the JSON-LD `keywords` on Article). Impact: Low. Effort: Low. (`src/components/SEO.tsx:110,25,39`) — added 2026-06-20
- [x] **[Cleaner code]** `hreflang` `en-US` + `x-default` both self-point on a single-language site — pure clutter; drop both (canonical already conveys the URL). Impact: Low. Effort: Low. (`src/components/SEO.tsx:113-114`) — added 2026-06-20 — **completed 2026-09-05** (both tags removed from SEO.tsx)
- [ ] **[Consistency]** `og:image:type` is never emitted though every static card is PNG — add `og:image:type=image/png` for the static path (omit/derive for the Sanity blog crops, whose format auto-negotiates). Impact: Low. Effort: Low. (`src/components/SEO.tsx:121-126`) — added 2026-06-20
- [ ] **[Consistency]** robots.txt AI allow-list names retired Anthropic tokens (`Claude-Web`, `Anthropic-AI`) + non-current `Cohere-ai` and omits the bots that actually drive citations (`ClaudeBot`, `Claude-User`, `Claude-SearchBot`, `OAI-SearchBot`, `Perplexity-User`, `Applebot-Extended`). Refresh the named set. Impact: Low. Effort: Low. (`public/robots.txt:25,28,34`) — added 2026-06-20
- [x] **[Cleaner code]** Sitemap stamps every static page with TODAY's build date on every build (lastmod inflation Google learns to distrust) while blog posts correctly use `_updatedAt` — drop `<lastmod>` for static pages or derive it from git. Impact: Low. Effort: Low. (`scripts/generate-sitemap.js:104,107-114`) — added 2026-06-20 — **completed 2026-09-05** (static lastmod now derives from each page's git commit date (VAL-SEO-003))
- [ ] **[Cleaner code]** robots.txt `Disallow: /api/` and `Disallow: /*.json$` match nothing on this origin (dead rules; `/*.json$` is a latent footgun if a `site.webmanifest` is added). Remove `/api/`; narrow the json rule. Impact: Low. Effort: Low. (`public/robots.txt:41-42`) — added 2026-06-20
- [ ] **[UX]** Eight meta descriptions exceed ~160 chars and truncate in SERPs/cards (Contact 213, Claude 207, Podcast 200, Foundation 199, Blueprint 192, Altivum 189, About 183, Beyond-the-Assessment 181) — trim and front-load the hook (worst offenders bury the CTA). Impact: Low. Effort: Low. (`src/pages/Contact.tsx:179` + 7 others) — added 2026-06-20
- [x] **[UX]** `/privacy` description is 97 chars of boilerplate — expand to ~150 with concrete detail (cookie-free analytics, 90-day chat-memory TTL); keep in sync with the duplicate string in its WebPage schema. Impact: Low. Effort: Low. (`src/pages/Privacy.tsx:31,40`) — added 2026-06-20 — **completed 2026-09-05** (expanded to 158 chars in 1fe4886)
- [x] **[Performance]** Dead `preconnect` to `www.buzzsprout.com` on every page (origin never requested anywhere) — also a dead CSP `frame-src` entry; remove both. Impact: Low. Effort: Low. (`index.html:47`, `customHttp.yml:47`) — added 2026-06-20 — **completed 2026-09-05** (dead preconnect removed from index.html)
- [ ] **[Performance]** Single-route origins (cognito + KB-builder, both `/admin`-only) are eagerly `preconnect`ed site-wide, crowding the ~6-connection budget against critical-path origins — downgrade to `dns-prefetch` or move to the consuming route. Impact: Low. Effort: Low. (`index.html:45-49`) — added 2026-06-20
- [ ] **[Performance]** The third-party media origins `/podcast` actually loads (`i.ytimg.com`, `www.youtube.com`, `open.spotify.com`) have no resource hints — add route-scoped `dns-prefetch` (reusing the budget freed by the dead preconnects). Impact: Low. Effort: Low. (`index.html:38-52`) — added 2026-06-20
- [ ] **[Performance]** LCP hero (hashed `src/assets/hero2.png`) has no `<link rel="preload">` — marginal: the `<img>` IS present in the prerendered body so the preload scanner already finds it; a head preload buys ~9KB-earlier discovery only. Low-value polish. Impact: Low. Effort: Low. (`src/pages/Home.tsx:7,137-144`, `index.html:38-52`) — added 2026-06-20
- [ ] **[Architecture]** The WebPage↔BreadcrumbList back-reference is wired by hand twice; AWS/Claude/Blueprint pass `breadcrumbs` to `<SEO>` but not to `buildWebPageSchema`, so 3 of 5 WebPage nodes never reference the BreadcrumbList beside them. Centralize the link in SEO.tsx. Impact: Low. Effort: Low. (`src/components/SEO.tsx:66-68`, `src/utils/schemas.ts:238-240`) — added 2026-06-20
- [ ] **[Consistency]** Canonical origin is hardcoded as three independent `SITE_URL` sources + inline literals — hoist to one shared `siteConfig` (the codebase centralizes paths in routes.ts; origin should match). Impact: Low. Effort: Low. (`src/utils/schemas.ts:7`, `src/utils/ogCards.ts:16`, `src/components/SEO.tsx:41,136`) — added 2026-06-20
- [ ] **[Cleaner code]** `structuredData` is typed via an inline intersection OUTSIDE `SEOProps` as weak `Record<string,unknown>[]` though 14 pages use it — move into the interface and require `@type` per entry. Impact: Low. Effort: Low. (`src/components/SEO.tsx:22-34,48-49`) — added 2026-06-20
- [ ] **[Cleaner code]** `buildWebPageSchema` emits an explicit `breadcrumb: undefined` key when no breadcrumbs — use the conditional-spread idiom the file already uses elsewhere (`...(opt ? {} : {})`). Impact: Low. Effort: Low. (`src/utils/schemas.ts:238-240`) — added 2026-06-20
- [ ] **[Cleaner code]** Repeated literal blocks in schemas.ts — `areaServed`-US ×3, `inLanguage:"en-US"` ×5, overlapping `knowsAbout` arrays — hoist invariants to shared constants. Impact: Low. Effort: Low. (`src/utils/schemas.ts:150-153,329-332,564-567`) — added 2026-06-20
- [x] **[Resilience & tests]** Three live builders are completely untested (`buildVideoObjectSchema`, `buildItemListSchema`, `buildFoundationOrganizationSchema`) + `foundationFAQs` — add describe blocks incl. BOTH branches of VideoObject's `uploadDate` spread and ItemList position indexing. Impact: Low. Effort: Low. (`src/utils/schemas.test.ts:2-22`) — added 2026-06-20 — **completed 2026-09-05** (all three builders now covered in schemas.test.ts)
- [ ] **[Resilience & tests]** The risky `url`-default→homepage-canonical path is exercised but never asserted — add a test that `<SEO>` with no `url` yields canonical/og:url/hreflang = the homepage (guards against an accidental homepage-canonical-on-a-subpage regression). Impact: Low. Effort: Low. (`src/components/SEO.tsx:41`, `src/components/SEO.test.tsx:77-83`) — added 2026-06-20
- [ ] **[Resilience & tests]** The custom-structuredData merge is only substring-checked — parse the emitted script and assert the 3 default nodes (Person/Org/WebSite) survive + exact length, pinning append-not-replace semantics. Impact: Low. Effort: Low. (`src/components/SEO.tsx:82-84`, `src/components/SEO.test.tsx:96-104`) — added 2026-06-20

### 45. Sanity content data layer

- [x] **[Observability & cost]** Every Sanity failure path dead-ends at console.error — an outage or CMS drift raises no alert — All three consumers of this data layer catch their failures, classify them, and hand them to createLogger. In production that logger writes a JSON line to the browser console (logger.ts:100-117), pushes a breadcrumb into a 20-item in-memory ring buffer (rum.ts:70-75), and stops. Impact: Medium. Effort: Low. (`src/sanity/errors.ts:18`, `src/pages/Blog.tsx:89`, `src/pages/BlogPost.tsx:183`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** Hover prefetch writes unvalidated posts into postCache and swallows its own failures — BlogPost validates a fetched post with isSanityPost before caching it (BlogPost.tsx:168-174) and logs `shape_validation_failed` on drift. Impact: Medium. Effort: Low. (`src/pages/Blog.tsx:173`, `src/pages/Blog.tsx:175`, `src/pages/BlogPost.tsx:168`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** classifySanityError never sees the error @sanity/client actually throws on a network failure — every offline/CORS blip logs as 'unknown' — `classifySanityError` matches on hand-guessed message text. In the browser, `@sanity/client` uses get-it's XHR adapter, whose network/CORS failure is `new Error("Request error while attempting to reach is https://k5950b3w.apicdn.sanity.io/...")` carrying `isNetworkError: true`. Impact: Medium. Effort: Low. (`src/sanity/errors.ts:55`, `src/sanity/errors.ts:60`, `src/sanity/errors.ts:63`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** The Sanity layer exports primitives but owns no fetch boundary, so three pages hand-assemble the same pipeline and two hand-build SanityError literals that bypass errors.ts — src/sanity ships client + query + guard + classifier + cache as separate parts and stops there, so Blog, BlogPost and Podcast each re-assemble the identical fetch → shape-guard → classify → log sequence. Impact: Low. Effort: Medium. (`src/pages/Blog.tsx:74`, `src/pages/BlogPost.tsx:165`, `src/pages/Podcast.tsx:69`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** cache.ts and postCache.ts duplicate the same TTL cache with divergent APIs; the missing reset makes cache.test.ts order-dependent — Two modules implement byte-for-byte the same TTL-expiry logic — read entry, compare Date.now() against fetchedAt, evict and return null on expiry — one over a single slot, one over a Map. Impact: Low. Effort: Low. (`src/sanity/cache.ts:9`, `src/sanity/postCache.ts:10`, `src/sanity/postCache.ts:21`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** queries.ts repeats the image and category projections four and three times, and duplicates the post-preview projection between the listing and relatedPosts — The same GROQ sub-projections are typed out verbatim across an 87-line file: the dereferenced image projection appears four times, the category coalesce three times, and the listing's post-card projection is re-typed inside POST_BY_SLUG_QUERY's relatedPosts. Impact: Low. Effort: Low. (`src/sanity/queries.ts:14`, `src/sanity/queries.ts:42`, `src/sanity/queries.ts:63`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** postCache has a second, unvalidated writer — the hover prefetch bypasses isSanityPost, and BlogPost renders cache hits without re-checking — The guards exist specifically so a CMS schema drift cannot reach the cache or render (guards.ts:5-7). BlogPost's direct fetch honours that: it runs isSanityPost before setPost/setPostCache. Impact: Low. Effort: Low. (`src/sanity/guards.ts:5`, `src/pages/Blog.tsx:171`, `src/pages/Blog.tsx:173`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** classifySanityError never matches the errors @sanity/client's browser transport actually throws — The `network` branch matches err.message against /network|fetch|failed to fetch|enotfound|econn|dns/. The installed transport (get-it 8.8.0, used by @sanity/client 7.22.1) picks XHR whenever XMLHttpRequest exists — i.e. Impact: Low. Effort: Low. (`src/sanity/errors.ts:60`, `src/sanity/errors.ts:55`, `src/sanity/errors.ts:47`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** The blog listing query fetches two document collections nothing reads — BLOG_LISTING_QUERY pulls a full tag collection and a full series collection alongside the posts, and both are typed, runtime-validated and cached — but no component ever reads them. Impact: Low. Effort: Medium. (`src/sanity/queries.ts:22`, `src/sanity/queries.ts:23`, `src/pages/Blog.tsx:83`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** One post missing an excerpt or category rejects the entire blog listing — the guard is stricter than the schema this repo declares — `hasPostPreviewCore` hard-requires `excerpt` and `category` to be strings, and `isBlogListingResult` runs it over every post with `.every(...)`. Impact: Low. Effort: Medium. (`src/sanity/guards.ts:47`, `src/sanity/guards.ts:74`, `src/sanity/schemas.ts:126`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The hover-prefetch path writes to postCache without running isSanityPost, so the shape guard is bypassed on the primary navigation route — BlogPost validates a fetched post with `isSanityPost` before caching and rendering it, but Blog's card-hover prefetch fetches the same query and calls `setPostCache(slug, data)` on any truthy value, with no guard. Impact: Low. Effort: Low. (`src/pages/Blog.tsx:173`, `src/sanity/postCache.ts:17`, `src/pages/BlogPost.tsx:148`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The schema drift test cannot detect a field added to a GROQ projection, and one of its assertions is a tautology — schemas.ts documents itself as being validated by a test that "asserts every projected field is declared, so the schema cannot drift from the queries". Impact: Low. Effort: Medium. (`src/sanity/schemas.ts:11`, `src/sanity/schemas.test.ts:96`, `src/sanity/schemas.test.ts:115`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** The hover-prefetch path writes unvalidated posts into postCache, so isSanityPost is fully bypassable — BlogPost only runs the `isSanityPost` shape guard on its own network fetch; the cache-hit path returns whatever is stored and renders it directly. Impact: Low. Effort: Low. (`src/pages/Blog.tsx:173`, `src/sanity/postCache.ts:17`, `src/pages/BlogPost.tsx:148`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** isBlogListingResult element-validates only `posts` — tags and series pass on Array.isArray alone — `isBlogListingResult` checks that `posts`, `tags` and `series` are arrays but only runs a per-element guard over `posts`. The top-level `tags`/`series` arrays and each post's own `tags`/`series` (projected by GROQ dereferences) are never inspected. Impact: Low. Effort: Low. (`src/sanity/guards.ts:74`, `src/sanity/guards.ts:47`, `src/sanity/guards.ts:4`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** isRenderableImageSource accepts any string _ref, so it does not actually gate urlFor as documented — The permissive image guard's `_ref` branch checks only `typeof asset._ref === 'string'`, never that the string is a well-formed Sanity image id. Impact: Low. Effort: Low. (`src/sanity/guards.ts:44`, `src/sanity/guards.ts:36`, `src/sanity/PortableTextComponents.tsx:75`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** resolveInternalPath treats every non-http scheme as an internal path, contradicting its own docblock — The internal/external test is `!href.startsWith('http')`, so anything that is not literally http-prefixed is returned as an internal route path. The docblock immediately above states the opposite — that `mailto:` and friends are external and return null. Impact: Low. Effort: Low. (`src/sanity/PortableTextComponents.tsx:30`, `src/sanity/PortableTextComponents.tsx:21`, `src/sanity/PortableTextComponents.tsx:251`) — added 2026-09-05 — **completed 2026-09-05**

### 46. Blog preview & editorial tooling

- [x] **[Architecture & maintainability]** generate-blog-previews.js overwrites a git-tracked data file with an empty array on any Sanity hiccup — src/data/generatedBlogPreviews.json is a tracked file (18 entries today) that Blog.tsx imports to guarantee CollectionPage `hasPart` references exist in the prerendered HTML. Impact: Medium. Effort: Low. (`scripts/generate-blog-previews.js:44`, `scripts/generate-blog-previews.js:48`, `scripts/generate-blog-previews.js:12`) — added 2026-09-05 — **completed 2026-09-05**
- [ ] **[Performance]** generatedBlogPreviews feeds only JSON-LD, so the prerendered /blog body is six aria-hidden skeletons and no posts — generate-blog-previews.js projects only `title` + `slug` (scripts/generate-blog-previews.js:35-38), and Blog.tsx consumes that file at exactly one place — the CollectionPage `hasPart` array (src/pages/Blog.tsx:207). Impact: Medium. Effort: Medium. (`scripts/generate-blog-previews.js:35`, `scripts/generate-blog-previews.js:48`, `src/pages/Blog.tsx:207`) _(gap sweep)_ — added 2026-09-05 — **deferred 2026-09-05**: needs the build-time generator widened AND Blog.tsx reseeded in one change; landing either half alone silently breaks the CollectionPage JSON-LD slug shape.
- [x] **[Architecture & maintainability]** rewrite-blog-headings.mjs is a spent one-shot migration that still force-overwrites live Sanity headings — The script is a 649-line file of which 416 lines (REWRITES at :55-412, INSERTIONS at :418-475) are hardcoded per-document data — 42 rewrites and 8 insertions naming specific Sanity doc IDs, block keys and before/after heading strings. Impact: Low. Effort: Low. (`scripts/rewrite-blog-headings.mjs:55`, `scripts/rewrite-blog-headings.mjs:418`, `scripts/rewrite-blog-headings.mjs:31`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** A fifth copy of the build-time Sanity client, when this repo's convention is to export the shared piece from one script — generate-blog-previews.js declares its own `createClient({ projectId: 'k5950b3w', dataset: 'production', apiVersion: '2024-01-01', useCdn: false, timeout: 15000 })` plus its own post query. Impact: Low. Effort: Low. (`scripts/generate-blog-previews.js:26`, `scripts/generate-sitemap.js:17`, `scripts/generate-rss.js:15`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** A Sanity blip makes generate-blog-previews overwrite the committed previews with [], and every gate that would catch it is non-fatal — `main()` catches a failed Sanity fetch, warns, and then falls through to the same unconditional `writeFileSync` with `posts` still `[]` — so a transient timeout (15s client timeout) replaces the 18 git-tracked previews with an empty array and exits 0. Impact: Low. Effort: Low. (`scripts/generate-blog-previews.js:43`, `scripts/generate-blog-previews.js:44`, `scripts/generate-blog-previews.js:50`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** A mid-run patch failure aborts rewrite-blog-headings with production already partially mutated and no report written — Roughly 46 rewrites and insertions are committed one document at a time with no per-item try/catch and no retry, while the patch report — the only record of what changed and what the prior text was — is written after every mutation has completed. Impact: Low. Effort: Low. (`scripts/rewrite-blog-headings.mjs:521`, `scripts/rewrite-blog-headings.mjs:566`, `scripts/rewrite-blog-headings.mjs:633`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** rewrite-blog-headings overwrites live heading text whenever it diverges from the hardcoded snapshot, and targets blocks by _key with no type/style check — The rewrite loop locates a block by `_key` alone and, when the current text does not match the `before` recorded in the plan, deliberately writes anyway — it just records `actualBefore` in the report. Impact: Low. Effort: Low. (`scripts/rewrite-blog-headings.mjs:506`, `scripts/rewrite-blog-headings.mjs:512`, `scripts/rewrite-blog-headings.mjs:516`) — added 2026-09-05 — **completed 2026-09-05**

### 47. Generative UI blocks

- [x] **[Consistency with existing patterns]** The gen-ui Opus path is the one model call in the repo with no guardrail, because it returns before the guardrail is ever wired — Every other Bedrock invocation in this codebase passes through the shared guardrail: the chat agent gets `guardrailConfig` on the BedrockModel, and the blueprint Lambda runs a dedicated `applyInputGuardrail` INPUT pre-check (deliberately INPUT-only, because guarding Opus generation false-blocked legitimate output). Impact: Medium. Effort: Medium. (`lambda/chat-stream/index.mjs:293`, `lambda/chat-stream/index.mjs:319`, `lambda/chat-stream/index.mjs:341`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** render_ui accepts any shape-valid path while navigate_to validates against the canonical route allowlist — `navigate_to` — the other tool that emits a site path chosen by the model — hard-rejects `/admin` and `/chat` and then runs `isValidPath(path)` against VALID_PATHS + BLOG_SLUG_PATTERN, so a hallucinated route is refused with a message the model can recover from. Impact: Medium. Effort: Low. (`lambda/chat-stream/uiBlocks.mjs:87`, `lambda/chat-stream/uiBlocks.mjs:65`, `lambda/chat-stream/tools/navigate.mjs:23`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** The gen-ui Opus path has zero alarms and its spend is invisible to the only Bedrock cost alarm — renderGenUi is the chat Lambda's only Opus call and it records its own metric names (GenUiOpusInputTokens/GenUiOpusOutputTokens, GenUiError, GenUiNoTool, GenUiInvalidBlocks), but setup-alarms.sh defines no alarm on any GenUi* metric — the chat cost alarm watches BedrockInputTokens, which the gen-ui path never writes. Impact: Medium. Effort: Low. (`lambda/chat-stream/genUi.mjs:135`, `scripts/setup-alarms.sh:114`, `scripts/setup-alarms.sh:118`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The gen-ui branch invokes Opus with no guardrail, and its test asserts every field of the command except that one — When a visitor on /chat types anything matching /\bgen[\s_-]?ui\b/, index.mjs short-circuits the whole Strands agent and calls Bedrock Converse directly from genUi.mjs. Impact: Medium. Effort: Medium. (`lambda/chat-stream/index.mjs:293`, `lambda/chat-stream/genUi.mjs:90`, `lambda/chat-stream/agent.mjs:47`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** The gen-ui Opus path calls Bedrock with no guardrail at all — neither guardrailConfig nor an ApplyGuardrail input pre-check — Every normal chat turn runs through a Bedrock model built with the site guardrail attached (index.mjs:341-342 → agent.mjs:46-50), and index.mjs:383-394 handles interventions. Impact: Medium. Effort: Medium. (`lambda/chat-stream/genUi.mjs:90`, `lambda/chat-stream/genUi.mjs:98`, `lambda/chat-stream/index.mjs:293`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** The gen-ui Opus path is metered by the Haiku-sized chat rate limit and is invisible to the only chat cost alarm — A single gen-ui request forces an Opus 4.6 Converse call with maxTokens 1500, on an endpoint whose legacy auth key ships in the frontend bundle. Impact: Medium. Effort: Low. (`lambda/chat-stream/genUi.mjs:20`, `lambda/chat-stream/genUi.mjs:98`, `lambda/chat-stream/genUi.mjs:135`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** The explicit gen-ui command sits behind an unchanged typing indicator for up to 20 seconds — A "gen-ui …" message is routed to a non-streaming Opus ConverseCommand with maxTokens 1500 and a 20s abort. Impact: Medium. Effort: Low. (`lambda/chat-stream/index.mjs:293`, `lambda/chat-stream/index.mjs:296`, `lambda/chat-stream/genUi.mjs:100`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** The hand-synced frontend type mirror has no drift guard, unlike the repo's other two hand-synced mirrors — src/utils/uiBlocks.ts declares itself "Kept in sync by hand" with lambda/chat-stream/uiBlocks.mjs, and today it is in sync. Impact: Low. Effort: Low. (`src/utils/uiBlocks.ts:4`, `src/components/chat/GenerativeBlocks.tsx:228`, `src/components/chat/GenerativeBlocks.test.tsx:112`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** renderGenUi's history parameter is dead in production but exercised by the test, and UI_BLOCK_TYPES is exported and never imported — `renderGenUi` accepts `history = []` and prepends it to the Converse messages, but the only production caller does not pass it — index.mjs:298-307 supplies bedrockClient, ConverseCommand, userMessage, retrievedContext, responseStream, metrics, requestId and abortSignal, and nothing else. Impact: Low. Effort: Low. (`lambda/chat-stream/genUi.mjs:79`, `lambda/chat-stream/genUi.mjs:86`, `lambda/chat-stream/index.mjs:298`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** A block the frontend does not recognise renders as a blank with no signal on either side — renderBlock's default arm returns null for an unknown type and LinkGrid returns null when the internal-path filter empties the list. Impact: Low. Effort: Low. (`src/components/chat/GenerativeBlocks.tsx:229`, `src/components/chat/GenerativeBlocks.tsx:185`, `src/components/chat/GenerativeBlocks.tsx:238`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** Gen-ui requests exit before TotalRequestLatency and the ChatMessageSent product event are recorded — The gen-ui branch ends the stream, flushes metrics and returns, skipping the tail of the handler entirely. The successful agent path records TotalRequestLatency, emits the ChatMessageSent product event with its latency, and awaits flushSentry/flushProductAnalytics;. Impact: Low. Effort: Low. (`lambda/chat-stream/index.mjs:317`, `lambda/chat-stream/index.mjs:319`, `lambda/chat-stream/index.mjs:407`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Every gen-ui request pays a DynamoDB memory query whose result is discarded — The handler loads visitor facts from DynamoDB before the gen-ui branch, but facts is consumed only by buildSystemPrompt, which sits below the branch and is never reached on a gen-ui turn. Impact: Low. Effort: Low. (`lambda/chat-stream/index.mjs:254`, `lambda/chat-stream/index.mjs:255`, `lambda/chat-stream/index.mjs:293`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** One throwing block latches GenerativeBlocks' ErrorBoundary for the whole message, and the boundary's fallback path is never tested — The ErrorBoundary wraps the entire blocks.map, and React error boundaries do not reset on prop change: once hasError latches, render() returns the empty fallback forever. Impact: Low. Effort: Low. (`src/components/chat/GenerativeBlocks.tsx:238`, `src/components/chat/GenerativeBlocks.tsx:44`, `src/components/ErrorBoundary.tsx:63`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The hand-synced frontend block mirror has no drift guard, unlike every other hand-synced pair in this repo — src/utils/uiBlocks.ts is documented as a hand-maintained mirror of the Zod vocabulary in lambda/chat-stream/uiBlocks.mjs, but nothing asserts the two agree. Impact: Low. Effort: Low. (`src/utils/uiBlocks.ts:3`, `lambda/chat-stream/uiBlocks.mjs:117`, `src/components/chat/GenerativeBlocks.tsx:228`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** renderGenUi's abort wiring and every failure path except schema-invalid blocks are untested; the fake Bedrock client discards the options argument — The 20s cancellation for the gen-ui Opus call depends entirely on the abortSignal being forwarded as the second argument to bedrockClient.send. Impact: Low. Effort: Low. (`lambda/chat-stream/genUi.mjs:100`, `lambda/chat-stream/index.mjs:296`, `lambda/chat-stream/index.mjs:31`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** genUi writes raw model text to the NUL-framed stream without the U+0000 strip agent.mjs applies, allowing a forged event frame — The wire protocol frames events as `\x00EVT\x00{json}\x00EVT\x00`. agent.mjs deliberately strips U+0000 from every model-text chunk before writing it, with a comment naming the exact threat ("A literal U+0000 in MODEL output could forge a frame"). Impact: Low. Effort: Low. (`lambda/chat-stream/genUi.mjs:127`, `lambda/chat-stream/genUi.mjs:103`, `lambda/chat-stream/agent.mjs:163`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** render_ui link paths are shape-checked but never whitelisted, and the client-side isInternalPath guard is bypassable by a backslash — link_grid.path and profile_mini.ctaPath are validated only by `^\/[a-z0-9/-]*$`, so the model can emit any path-shaped string — including /admin (the Cognito-protected KB console) and invented routes that dead-end on the 404 page. Impact: Low. Effort: Medium. (`lambda/chat-stream/uiBlocks.mjs:87`, `lambda/chat-stream/uiBlocks.mjs:65`, `lambda/chat-stream/tools/navigate.mjs:23`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UI]** Explainer blocks render the answer's prose at caption size, smaller than the chat text they supplement — The explainer block is the vocabulary's prose surface — up to three paragraphs of 400 characters plus five bullets, so ~1,400 characters of running text — but every paragraph and bullet is set with typography.smallText, the caption/label token (12px on mobile, 14px on desktop). Impact: Low. Effort: Low. (`src/components/chat/GenerativeBlocks.tsx:162`, `src/components/chat/GenerativeBlocks.tsx:170`, `lambda/chat-stream/uiBlocks.mjs:74`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UI]** render_ui has no human label, so the chat shows "Alti is render ui…" — Every Strands tool call emits a `tool_invocation` event carrying the raw tool name, and ChatMessage renders it as `Alti is {toolLabel(tool)}…` in a role="status" aria-live="polite" row. Impact: Low. Effort: Low. (`src/components/chat/ChatMessage.tsx:27`, `src/components/chat/ChatMessage.tsx:37`, `src/components/chat/ChatMessage.tsx:221`) — added 2026-09-05 — **completed 2026-09-05**

### 48. Podcast RAG / Ask the Vector

- [x] **[Accessibility]** Submitting destroys keyboard focus — the focused input and the clicked example-prompt button are both disabled or unmounted mid-interaction — `busy` flips true synchronously on send (line 33 combining useChatEngine.ts:168/193), which disables the input the user is typing in (line 84) and the submit button they just activated (line 91). Impact: Medium. Effort: Low. (`src/components/podcast/AskTheVector.tsx:84`, `src/components/podcast/AskTheVector.tsx:91`, `src/components/podcast/AskTheVector.tsx:101`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Accessibility]** The answer region is a polite live region wrapped in a conditional mount, so the first answer is never announced and later ones are re-announced on every streamed token — AskTheVector.tsx:124 puts a hardcoded `aria-live="polite"` on the results container, and that container is only mounted once `hasConversation` is true (line 123). Because the region enters the DOM at the same instant as its content, screen readers do not announce the first answer at all. Impact: Medium. Effort: Low. (`src/components/podcast/AskTheVector.tsx:123`, `src/components/podcast/AskTheVector.tsx:124`, `src/components/podcast/AskTheVector.tsx:144`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** Ask The Vector renders "last of each kind" across the whole session, so a follow-up question shows the previous answer and its citations — AskTheVector builds everything it displays by reversing the entire accumulated message list and running three independent `find`s for the last user, last non-system assistant, and last system message. Impact: Medium. Effort: Low. (`src/components/podcast/AskTheVector.tsx:35`, `src/components/podcast/AskTheVector.tsx:37`, `src/components/podcast/AskTheVector.tsx:38`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Architecture & maintainability]** search_podcast prompt instructions aren't gated on the tool actually being registered — `buildTools` drops `search_podcast` entirely when `deps.podcastKbId` is falsy (tools/index.mjs:24), and `PODCAST_KB_ID` is the only Bedrock/Sanity id in the handler that falls back to an empty string (index.mjs:50) instead of a live value like KB_ID (49), GUARDRAIL_ID (51) and SANITY_PROJECT_ID (36). Impact: Medium. Effort: Medium. (`lambda/chat-stream/index.mjs:50`, `lambda/chat-stream/index.mjs:49`, `lambda/chat-stream/index.mjs:61`) _(gap sweep)_ — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Consistency with existing patterns]** The podcast KB retriever skips the unit-test + live-contract-test pattern the bio KB retriever established — The bio KB retriever ships with two tests: a unit test with a fake Bedrock client, and a gated live contract test whose own header states it exists because a wrong KB id or an API shape change "would ship green and leave Alti silently un-grounded." The podcast retriever — a structurally identical second Bedrock KB added later — has neither. Impact: Medium. Effort: Low. (`lambda/chat-stream/__tests__/kb-retrieve-contract.test.mjs:9`, `lambda/chat-stream/__tests__/kb-retrieve-contract.test.mjs:26`, `lambda/chat-stream/__tests__/tools/searchPodcast.test.mjs:48`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Observability & cost]** A podcast KB outage is indistinguishable from "no episode covers that" — no alarm, no dashboard, and the visitor gets a confidently wrong answer — When the podcast Retrieve call errors or times out, retrievePodcastChunks swallows it and returns an empty array, so search_podcast returns `{ ok: true, results: [] }` — the exact same shape as a genuine no-match. Impact: Medium. Effort: Low. (`lambda/chat-stream/podcastRetrieve.mjs:93`, `lambda/chat-stream/tools/searchPodcast.mjs:86`, `lambda/chat-stream/podcastRetrieve.mjs:74`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** A KB outage is returned to the model as "no matches", and a test locks that behaviour in — retrievePodcastChunks returns the empty array for three semantically different outcomes: no PODCAST_KB_ID configured (:29), the KB genuinely matched nothing (:57), and Bedrock threw or timed out (:93). Impact: Medium. Effort: Low. (`lambda/chat-stream/podcastRetrieve.mjs:29`, `lambda/chat-stream/podcastRetrieve.mjs:93`, `lambda/chat-stream/tools/searchPodcast.mjs:85`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** Every follow-up question renders the PREVIOUS answer and its timestamp cards as the response, and no test sets a busy state — AskTheVector derives `lastAssistant` and `citations` by scanning the whole message list for the most recent non-system assistant message. Impact: Medium. Effort: Medium. (`src/components/podcast/AskTheVector.tsx:35`, `src/components/podcast/AskTheVector.tsx:37`, `src/components/podcast/AskTheVector.tsx:40`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** The de-dupe test uses four distinct episodes, hiding that live retrieval returns same-episode clusters — searchPodcast de-dupes on `${videoId}-${startSeconds}`, which only removes the exact same moment, not repeated hits within one episode. The dedupe test feeds vid1..vid4 — four different episodes — so it proves the cap but never exercises the shape the KB actually returns. Impact: Medium. Effort: Low. (`lambda/chat-stream/tools/searchPodcast.mjs:93`, `lambda/chat-stream/tools/searchPodcast.mjs:78`, `lambda/chat-stream/__tests__/tools/searchPodcast.test.mjs:149`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Resilience & tests]** podcastRetrieve.mjs has no test file at all — its 4s abort path is never exercised and the fakes ignore abortSignal — podcastRetrieve.mjs is the only retrieval module in chat-stream with no test file of its own; it is covered only transitively through searchPodcast.test.mjs. Impact: Medium. Effort: Medium. (`lambda/chat-stream/podcastRetrieve.mjs:47`, `lambda/chat-stream/podcastRetrieve.mjs:72`, `lambda/chat-stream/podcastRetrieve.mjs:84`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** The "isolated" ask box still carries the device id, so it reads and writes the 90-day visitor memory store with no disclosure on that surface — AskTheVector.tsx:20 documents the surface as isolated ("questions here never mix into the main Alti conversation"), but the isolation is only the sessionStorage transcript. Impact: Medium. Effort: Low. (`src/components/podcast/AskTheVector.tsx:20`, `src/components/podcast/AskTheVector.tsx:28`, `src/hooks/useChatEngine.ts:195`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** A system notice (rate limit, guardrail) sticks under every later answer for the rest of the tab session — `lastSystem` is the most recent `isSystem` message anywhere in the history (line 38), and it is rendered unconditionally whenever a conversation exists (line 163). System messages are appended by useChatEngine.ts:274-282 with an id of `system-${Date.now()}` and are never removed or superseded. Impact: Medium. Effort: Low. (`src/components/podcast/AskTheVector.tsx:38`, `src/components/podcast/AskTheVector.tsx:163`, `src/hooks/useChatEngine.ts:274`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[UX]** On the second and later questions the previous answer and its timestamp cards stay on screen under the new question, with no loading indicator — useChatEngine only creates the assistant message once the first output chunk arrives (`ensureMessage()` at useChatEngine.ts:313), while `setIsTyping(true)` fires immediately at line 168. Impact: Medium. Effort: Low. (`src/components/podcast/AskTheVector.tsx:37`, `src/components/podcast/AskTheVector.tsx:40`, `src/components/podcast/AskTheVector.tsx:136`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Cleaner code]** podcastRetrieve.mjs is a near-verbatim fork of kbRetrieve.mjs that has already drifted on failure metrics — `retrievePodcastChunks` reproduces `retrieveContext` almost line for line: same AbortController + setTimeout, same RetrieveCommand/vectorSearchConfiguration shape, same latency metric, same empty-results log, same AbortError-vs-other error branching. Impact: Low. Effort: Medium. (`lambda/chat-stream/podcastRetrieve.mjs:33`, `lambda/chat-stream/podcastRetrieve.mjs:37`, `lambda/chat-stream/kbRetrieve.mjs:71`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** Ask The Vector's session store grows without bound: every ask re-sends up to 20 invisible prior messages and every streamed chunk re-serializes the whole array — AskTheVector deliberately isolates its conversation into its own sessionStorage key and seeds it empty, but nothing ever trims it — messages are only ever appended, and the component never takes handleClearConversation. Impact: Low. Effort: Low. (`src/components/podcast/AskTheVector.tsx:28`, `src/components/podcast/AskTheVector.tsx:35`, `src/hooks/useChatEngine.ts:174`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Performance]** The statically imported ask box pulls the deliberately code-split chat engine into the /podcast route chunk for every visitor — ChatWidget explicitly code-splits the chat panel — and with it useChatEngine, the event parser, session token/Turnstile and ToolDraftCard — so that graph is not in any page's critical request chain until a visitor opens the widget. Impact: Low. Effort: Medium. (`src/pages/Podcast.tsx:35`, `src/components/podcast/AskTheVector.tsx:3`, `src/components/podcast/AskTheVector.tsx:5`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** The ask box input has no maxLength, so an over-length question burns a rate-limit slot before the server rejects it — The AskTheVector input at AskTheVector.tsx:78-88 sets no maxLength, while the main chat's ChatInput.tsx:84 sets maxLength={4000} to match the server contract (ChatInput.test.tsx:27 asserts it). Impact: Low. Effort: Low. (`src/components/podcast/AskTheVector.tsx:78`, `src/components/chat/ChatInput.tsx:84`, `lambda/chat-stream/validation.mjs:61`) — added 2026-09-05 — **completed 2026-09-05**
- [x] **[Security]** podcast_citation is the only draft card that hands a server-supplied absolute URL to window.open, and videoId is never validated at any layer — podcastRetrieve.mjs:67 accepts `meta.videoId` on nothing more than `typeof === "string"`, and searchPodcast.mjs:109 interpolates it raw into `https://www.youtube.com/watch?v=${c.videoId}&t=${c.startSeconds}s`. Impact: Low. Effort: Low. (`lambda/chat-stream/podcastRetrieve.mjs:67`, `lambda/chat-stream/podcastRetrieve.mjs:72`, `lambda/chat-stream/tools/searchPodcast.mjs:109`) — added 2026-09-05 — **completed 2026-09-05**
